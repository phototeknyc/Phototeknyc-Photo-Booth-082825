<Window x:Class="Photobooth.Windows.TemplateBrowserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Template Browser" 
        Height="700" Width="1000"
        Background="#2D2D30"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#E1E1E1"/>
        </Style>
        
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#3C3C3C"/>
            <Setter Property="Foreground" Value="#E1E1E1"/>
            <Setter Property="BorderBrush" Value="#555"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
        </Style>
        
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Background" Value="#0078D4"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="3"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#106EBE"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#005A9E"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
            <Setter Property="Background" Value="#5C5C5C"/>
        </Style>
        
        <Style x:Key="TemplateCard" TargetType="Border">
            <Setter Property="Background" Value="#3C3C3C"/>
            <Setter Property="BorderBrush" Value="#555"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect ShadowDepth="2" Opacity="0.3" BlurRadius="6"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#464646"/>
                    <Setter Property="BorderBrush" Value="#0078D4"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <!-- Template for template items -->
        <DataTemplate x:Key="TemplateItemTemplate">
            <Border Width="280" Height="220"
                    MouseLeftButtonDown="TemplateCard_MouseLeftButtonDown"
                    Tag="{Binding}">
                <Border.Style>
                    <Style TargetType="Border" BasedOn="{StaticResource TemplateCard}">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsLastUsed}" Value="True">
                                <Setter Property="BorderBrush" Value="#FFD700"/>
                                <Setter Property="BorderThickness" Value="2"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="140"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Preview Area -->
                    <Border Grid.Row="0" Background="#2A2A2A" Margin="8,8,8,4"
                            CornerRadius="4" ClipToBounds="True">
                        <Grid>
                            <!-- Thumbnail Image -->
                            <Image Name="ThumbnailImage" 
                                   Source="{Binding ThumbnailImageSource}" 
                                   Stretch="Uniform"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   Margin="2"/>
                            
                            <!-- Placeholder when no thumbnail -->
                            <Border Background="#1E1E1E">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding HasThumbnail}" Value="False">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <Path Data="M9,3V4H4V6H5V19A2,2 0 0,0 7,21H17A2,2 0 0,0 19,19V6H20V4H15V3H9M7,6H17V19H7V6M9,8V17H11V8H9M13,8V17H15V8H13Z" 
                                          Fill="#666" Width="32" Height="32" Stretch="Uniform"/>
                                    <TextBlock Text="No Preview" FontSize="10" Foreground="#666" 
                                               HorizontalAlignment="Center" Margin="0,4,0,0"/>
                                </StackPanel>
                            </Border>
                            
                            <!-- Size indicator -->
                            <Border Background="#000000AA" 
                                    HorizontalAlignment="Right" 
                                    VerticalAlignment="Top"
                                    CornerRadius="0,0,0,3"
                                    Padding="6,3">
                                <TextBlock Text="{Binding SizeText}" 
                                           FontSize="9" Foreground="White"/>
                            </Border>
                        </Grid>
                    </Border>
                    
                    <!-- Template Info -->
                    <Grid Grid.Row="1" Margin="12,4,12,8">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        
                        <!-- Template Name -->
                        <TextBlock Grid.Row="0" Text="{Binding Name}" 
                                   FontWeight="SemiBold" FontSize="13"
                                   TextTrimming="CharacterEllipsis"
                                   ToolTip="{Binding Name}"/>
                        
                        <!-- Description -->
                        <TextBlock Grid.Row="1" Text="{Binding Description}" 
                                   FontSize="10" Foreground="#AAA"
                                   TextTrimming="CharacterEllipsis"
                                   ToolTip="{Binding Description}"
                                   Margin="0,2,0,0"/>
                        
                        <!-- Date -->
                        <TextBlock Grid.Row="2" Text="{Binding CreatedDate, StringFormat='Created: {0:MMM dd, yyyy}'}" 
                                   FontSize="9" Foreground="#888"
                                   Margin="0,4,0,0"/>
                        
                        <!-- Action buttons -->
                        <StackPanel Grid.Row="3" Orientation="Horizontal" 
                                    HorizontalAlignment="Stretch" VerticalAlignment="Bottom"
                                    Margin="0,6,0,0">
                            <Button Style="{StaticResource ActionButton}"
                                    HorizontalAlignment="Stretch"
                                    Width="120" Height="28"
                                    Click="LoadTemplate_Click"
                                    Tag="{Binding}">
                                <StackPanel Orientation="Horizontal">
                                    <Path Data="M5,20H19V18H5M19,9H15V3H9V9H5L12,16L19,9Z" 
                                          Fill="White" Width="14" Height="14" 
                                          Stretch="Uniform" Margin="0,0,4,0"/>
                                    <TextBlock Text="LOAD" FontSize="11" FontWeight="Bold" 
                                               VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>
                            <Button Style="{StaticResource SecondaryButton}"
                                    Width="120" Height="28"
                                    Click="DeleteTemplate_Click"
                                    Tag="{Binding}">
                                <StackPanel Orientation="Horizontal">
                                    <Path Data="M19,4H15.5L14.5,3H9.5L8.5,4H5V6H19M6,19A2,2 0 0,0 8,21H16A2,2 0 0,0 18,19V7H6V19Z" 
                                          Fill="White" Width="14" Height="14" 
                                          Stretch="Uniform" Margin="0,0,4,0"/>
                                    <TextBlock Text="DELETE" FontSize="11" FontWeight="Bold" 
                                               VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </Grid>
                </Grid>
            </Border>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" Background="#383838" BorderBrush="#555" BorderThickness="0,0,0,1">
            <Grid Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <!-- Title and controls -->
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Column="0" Text="Template Browser" 
                               FontSize="20" FontWeight="Bold"
                               VerticalAlignment="Center"/>
                    
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button Content="Refresh" Style="{StaticResource SecondaryButton}"
                                Click="RefreshTemplates_Click"/>
                        <Button Content="Import" Style="{StaticResource ActionButton}"
                                Click="ImportTemplate_Click"/>
                    </StackPanel>
                </Grid>
                
                <!-- Search and filters -->
                <Grid Grid.Row="1" Margin="0,15,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <Grid Grid.Column="0" Margin="0,0,10,0">
                        <TextBox Name="SearchBox" 
                                 TextChanged="SearchBox_TextChanged"/>
                        <TextBlock IsHitTestVisible="False" 
                                   Text="Search templates..." 
                                   VerticalAlignment="Center" 
                                   HorizontalAlignment="Left" 
                                   Margin="9,0,0,0" 
                                   Foreground="#666">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Text, ElementName=SearchBox}" Value="">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>
                    
                    <TextBlock Grid.Column="1" Text="Sort by:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <ComboBox Grid.Column="2" Name="SortComboBox" 
                              Background="#3C3C3C" Foreground="#E1E1E1"
                              BorderBrush="#555" BorderThickness="1"
                              Padding="8,6" MinWidth="120"
                              SelectionChanged="SortComboBox_SelectionChanged">
                        <ComboBoxItem Content="Name" IsSelected="True"/>
                        <ComboBoxItem Content="Date Created"/>
                        <ComboBoxItem Content="Date Modified"/>
                    </ComboBox>
                </Grid>
            </Grid>
        </Border>
        
        <!-- Template Grid -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" 
                      Background="#2D2D30" Padding="12">
            <ItemsControl Name="TemplatesList" ItemTemplate="{StaticResource TemplateItemTemplate}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" 
                                   HorizontalAlignment="Center"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </ScrollViewer>
        
        <!-- Empty state -->
        <Border Grid.Row="1" Name="EmptyState" Visibility="Collapsed"
                Background="#2D2D30">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <Path Data="M12,2A2,2 0 0,1 14,4C14,4.74 13.6,5.39 13,5.73V7H14A7,7 0 0,1 21,14H22A1,1 0 0,1 23,15V18A1,1 0 0,1 22,19H21A7,7 0 0,1 14,26H13V21.27C13.6,21.61 14,22.26 14,23A2,2 0 0,1 12,25A2,2 0 0,1 10,23C10,22.26 10.4,21.61 11,21.27V5.73C10.4,5.39 10,4.74 10,4A2,2 0 0,1 12,2M12,3A1,1 0 0,0 11,4A1,1 0 0,0 12,5A1,1 0 0,0 13,4A1,1 0 0,0 12,3Z" 
                      Fill="#666" Width="48" Height="48" Stretch="Uniform"/>
                <TextBlock Text="No templates found" FontSize="18" Foreground="#666" 
                           HorizontalAlignment="Center" Margin="0,16,0,8"/>
                <TextBlock Text="Create your first template or import existing ones" 
                           FontSize="12" Foreground="#888"
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
        
        <!-- Footer -->
        <Border Grid.Row="2" Background="#383838" BorderBrush="#555" BorderThickness="0,1,0,0">
            <Grid Margin="20,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0" Name="StatusText" 
                           Text="Ready" FontSize="11" 
                           VerticalAlignment="Center"/>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="Close" Style="{StaticResource SecondaryButton}"
                            Click="Close_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>