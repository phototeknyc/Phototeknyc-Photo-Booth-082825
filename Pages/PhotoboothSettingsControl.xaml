<UserControl x:Class="Photobooth.Pages.PhotoboothSettingsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Photobooth.Pages"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    
    <UserControl.Background>
        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#0A0E27" Offset="0"/>
            <GradientStop Color="#1A1E37" Offset="0.5"/>
            <GradientStop Color="#0A0E27" Offset="1"/>
        </LinearGradientBrush>
    </UserControl.Background>
    
    <UserControl.Resources>
        <!-- Modern Glassmorphic Header Style -->
        <Style x:Key="SettingsHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="22"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Margin" Value="0,20,0,15"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#667EEA" BlurRadius="8" ShadowDepth="0" Opacity="0.3"/>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Modern Section Header with Icons -->
        <Style x:Key="SectionHeaderStyle" TargetType="StackPanel">
            <Setter Property="Orientation" Value="Horizontal"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
        </Style>
        
        <Style x:Key="SectionIconStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="Foreground" Value="#667EEA"/>
            <Setter Property="Margin" Value="0,0,15,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#667EEA" BlurRadius="10" ShadowDepth="0" Opacity="0.5"/>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="SectionTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        
        <!-- Modern Text Style -->
        <Style x:Key="SettingsTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Foreground" Value="#E0E6ED"/>
            <Setter Property="Margin" Value="0,8,0,8"/>
            <Setter Property="FontWeight" Value="Medium"/>
        </Style>
        
        <!-- Touch-Friendly Slider Style -->
        <Style x:Key="SettingsSliderStyle" TargetType="Slider">
            <Setter Property="Margin" Value="0,8,0,20"/>
            <Setter Property="Height" Value="48"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Slider">
                        <Grid>
                            <Border x:Name="TrackBackground"
                                    Height="8"
                                    Background="#2D3748"
                                    CornerRadius="4"
                                    VerticalAlignment="Center">
                                <Border.Effect>
                                    <DropShadowEffect Color="Black" BlurRadius="4" ShadowDepth="2" Opacity="0.3"/>
                                </Border.Effect>
                            </Border>
                            <Border x:Name="PART_SelectionRange"
                                    Height="8"
                                    CornerRadius="4"
                                    VerticalAlignment="Center"
                                    HorizontalAlignment="Left">
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                        <GradientStop Color="#667EEA" Offset="0"/>
                                        <GradientStop Color="#764BA2" Offset="1"/>
                                    </LinearGradientBrush>
                                </Border.Background>
                                <Border.Effect>
                                    <DropShadowEffect Color="#667EEA" BlurRadius="8" ShadowDepth="0" Opacity="0.6"/>
                                </Border.Effect>
                            </Border>
                            <Thumb x:Name="PART_Thumb"
                                   Width="32"
                                   Height="32"
                                   Style="{DynamicResource ModernSliderThumb}"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern Slider Thumb -->
        <Style x:Key="ModernSliderThumb" TargetType="Thumb">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Ellipse x:Name="ThumbEllipse" 
                                 Fill="White" 
                                 Stroke="#667EEA" 
                                 StrokeThickness="3"
                                 Width="32" 
                                 Height="32"
                                 RenderTransformOrigin="0.5,0.5">
                            <Ellipse.Effect>
                                <DropShadowEffect Color="#667EEA" BlurRadius="12" ShadowDepth="0" Opacity="0.8"/>
                            </Ellipse.Effect>
                        </Ellipse>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ThumbEllipse" Property="Fill" Value="#F7FAFC"/>
                                <Setter TargetName="ThumbEllipse" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#667EEA" BlurRadius="16" ShadowDepth="0" Opacity="1"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsDragging" Value="True">
                                <Setter TargetName="ThumbEllipse" Property="Fill" Value="#EDF2F7"/>
                                <Setter TargetName="ThumbEllipse" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="1.1" ScaleY="1.1"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Modern TextBox Style -->
        <Style x:Key="SettingsTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Color="#2D3748" Offset="0"/>
                        <GradientStop Color="#1A202C" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="BorderBrush" Value="#4A5568"/>
            <Setter Property="Padding" Value="16,12"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Margin" Value="0,8,0,20"/>
            <Setter Property="Height" Value="48"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="24"
                                Padding="{TemplateBinding Padding}">
                            <Border.Effect>
                                <DropShadowEffect Color="Black" BlurRadius="8" ShadowDepth="4" Opacity="0.2"/>
                            </Border.Effect>
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="#667EEA"/>
                                <Setter TargetName="Border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#667EEA" BlurRadius="12" ShadowDepth="0" Opacity="0.4"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="#667EEA"/>
                                <Setter TargetName="Border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#667EEA" BlurRadius="16" ShadowDepth="0" Opacity="0.6"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Modern CheckBox Style -->
        <Style x:Key="SettingsCheckBoxStyle" TargetType="CheckBox">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Margin" Value="0,10,0,20"/>
            <Setter Property="MinHeight" Value="48"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal" Background="Transparent">
                            <Border x:Name="CheckBorder"
                                    Width="32"
                                    Height="32"
                                    CornerRadius="8"
                                    BorderThickness="2"
                                    BorderBrush="#4A5568"
                                    Margin="0,0,16,0"
                                    VerticalAlignment="Center">
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                        <GradientStop Color="#2D3748" Offset="0"/>
                                        <GradientStop Color="#1A202C" Offset="1"/>
                                    </LinearGradientBrush>
                                </Border.Background>
                                <Border.Effect>
                                    <DropShadowEffect Color="Black" BlurRadius="4" ShadowDepth="2" Opacity="0.3"/>
                                </Border.Effect>
                                <Path x:Name="CheckMark"
                                      Visibility="Collapsed"
                                      Stroke="White"
                                      StrokeThickness="3"
                                      Data="M8,14 L12,18 L20,10"
                                      Stretch="Uniform"
                                      Margin="6"/>
                            </Border>
                            <ContentPresenter VerticalAlignment="Center"
                                              TextBlock.Foreground="{TemplateBinding Foreground}"
                                              TextBlock.FontSize="{TemplateBinding FontSize}"
                                              TextBlock.FontWeight="{TemplateBinding FontWeight}"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckBorder" Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#667EEA" Offset="0"/>
                                            <GradientStop Color="#764BA2" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="CheckBorder" Property="BorderBrush" Value="#667EEA"/>
                                <Setter TargetName="CheckBorder" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#667EEA" BlurRadius="12" ShadowDepth="0" Opacity="0.6"/>
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CheckBorder" Property="BorderBrush" Value="#667EEA"/>
                                <Setter TargetName="CheckBorder" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#667EEA" BlurRadius="8" ShadowDepth="0" Opacity="0.4"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern ComboBox Style -->
        <Style x:Key="SettingsComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Height" Value="48"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Margin" Value="0,8,0,20"/>
            <Setter Property="Padding" Value="16,12"/>
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Color="#2D3748" Offset="0"/>
                        <GradientStop Color="#1A202C" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="BorderBrush" Value="#4A5568"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton"
                                          Grid.Column="2"
                                          Focusable="False"
                                          ClickMode="Press"
                                          IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="Border"
                                                Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="24">
                                            <Border.Effect>
                                                <DropShadowEffect Color="Black" BlurRadius="8" ShadowDepth="4" Opacity="0.2"/>
                                            </Border.Effect>
                                            <Grid>
                                                <ContentPresenter Margin="16,0,40,0" 
                                                                  VerticalAlignment="Center" 
                                                                  HorizontalAlignment="Left"/>
                                                <Path x:Name="Arrow"
                                                      HorizontalAlignment="Right"
                                                      Margin="0,0,16,0"
                                                      VerticalAlignment="Center"
                                                      Data="M0,0 L6,6 L12,0"
                                                      Fill="#667EEA"
                                                      Stroke="#667EEA"
                                                      StrokeThickness="1"/>
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Border" Property="BorderBrush" Value="#667EEA"/>
                                                <Setter TargetName="Border" Property="Effect">
                                                    <Setter.Value>
                                                        <DropShadowEffect Color="#667EEA" BlurRadius="12" ShadowDepth="0" Opacity="0.4"/>
                                                    </Setter.Value>
                                                </Setter>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="ContentSite"
                                              IsHitTestVisible="False"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                              Margin="16,0,40,0"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Left"/>
                            <Popup x:Name="Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Slide">
                                <Border x:Name="DropDownBorder"
                                        Background="#2D3748"
                                        BorderBrush="#667EEA"
                                        BorderThickness="2"
                                        CornerRadius="12"
                                        MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                        MinWidth="{TemplateBinding ActualWidth}">
                                    <Border.Effect>
                                        <DropShadowEffect Color="Black" BlurRadius="16" ShadowDepth="8" Opacity="0.4"/>
                                    </Border.Effect>
                                    <ScrollViewer>
                                        <StackPanel IsItemsHost="True"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern Button Style -->
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="48"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="24"
                                Padding="{TemplateBinding Padding}">
                            <Border.Effect>
                                <DropShadowEffect Color="Black" BlurRadius="8" ShadowDepth="4" Opacity="0.3"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="ButtonBorder"
                                                             Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleX)"
                                                             To="1.05" Duration="0:0:0.1"/>
                                            <DoubleAnimation Storyboard.TargetName="ButtonBorder"
                                                             Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleY)"
                                                             To="1.05" Duration="0:0:0.1"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="ButtonBorder"
                                                             Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleX)"
                                                             To="1.0" Duration="0:0:0.1"/>
                                            <DoubleAnimation Storyboard.TargetName="ButtonBorder"
                                                             Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleY)"
                                                             To="1.0" Duration="0:0:0.1"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                                <Setter TargetName="ButtonBorder" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="1" ScaleY="1"/>
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="ButtonBorder" Property="RenderTransformOrigin" Value="0.5,0.5"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.95" ScaleY="0.95"/>
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="ButtonBorder" Property="RenderTransformOrigin" Value="0.5,0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Glassmorphic Section Border Style -->
        <Style x:Key="GlassSectionStyle" TargetType="Border">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1" Opacity="0.1">
                        <GradientStop Color="White" Offset="0"/>
                        <GradientStop Color="Transparent" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="BorderBrush">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#667EEA" Offset="0" />
                        <GradientStop Color="#764BA2" Offset="1" />
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="25"/>
            <Setter Property="Padding" Value="30"/>
            <Setter Property="Margin" Value="0,0,0,25"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" BlurRadius="20" ShadowDepth="8" Opacity="0.3"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Action Button Styles -->
        <Style x:Key="SaveButtonStyle" TargetType="Button" BasedOn="{StaticResource ModernButtonStyle}">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#48BB78" Offset="0"/>
                        <GradientStop Color="#38A169" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="Padding" Value="32,12"/>
            <Setter Property="FontSize" Value="18"/>
        </Style>

        <Style x:Key="ResetButtonStyle" TargetType="Button" BasedOn="{StaticResource ModernButtonStyle}">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#F56565" Offset="0"/>
                        <GradientStop Color="#E53E3E" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="Padding" Value="32,12"/>
            <Setter Property="FontSize" Value="18"/>
        </Style>

        <Style x:Key="OpenButtonStyle" TargetType="Button" BasedOn="{StaticResource ModernButtonStyle}">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#667EEA" Offset="0"/>
                        <GradientStop Color="#764BA2" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="Padding" Value="32,12"/>
            <Setter Property="FontSize" Value="18"/>
        </Style>
    </UserControl.Resources>
    
    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="40">
        <StackPanel>
            <TextBlock Text="⚙️ Photobooth Settings" 
                       FontSize="28" 
                       FontWeight="Bold" 
                       Foreground="White" 
                       Margin="0,0,0,40"
                       HorizontalAlignment="Center">
                <TextBlock.Effect>
                    <DropShadowEffect Color="#667EEA" BlurRadius="12" ShadowDepth="0" Opacity="0.6"/>
                </TextBlock.Effect>
            </TextBlock>
            
            <!-- Countdown Timer Settings -->
            <Border Style="{StaticResource GlassSectionStyle}">
                <StackPanel>
                    <StackPanel Style="{StaticResource SectionHeaderStyle}">
                        <TextBlock Text="⏱️" Style="{StaticResource SectionIconStyle}"/>
                        <TextBlock Text="Countdown Timer" Style="{StaticResource SectionTitleStyle}"/>
                    </StackPanel>
                    
                    <TextBlock Text="Default countdown duration (1-10 seconds)" Style="{StaticResource SettingsTextStyle}"/>
                    <StackPanel Orientation="Horizontal" Margin="0,8,0,20">
                        <Slider x:Name="countdownSlider" 
                                Minimum="1" 
                                Maximum="10" 
                                Value="5" 
                                TickPlacement="BottomRight" 
                                TickFrequency="1" 
                                IsSnapToTickEnabled="True"
                                Width="300"
                                ValueChanged="CountdownSlider_ValueChanged"
                                Style="{StaticResource SettingsSliderStyle}"/>
                        <TextBlock x:Name="countdownValueText" 
                                   Text="5 seconds" 
                                   VerticalAlignment="Center" 
                                   Margin="20,0,0,0" 
                                   Foreground="White" 
                                   FontWeight="Bold"
                                   FontSize="18"/>
                    </StackPanel>
                    
                    <CheckBox x:Name="showCountdownCheckBox" 
                              Content="Show visual countdown overlay" 
                              IsChecked="True"
                              Style="{StaticResource SettingsCheckBoxStyle}"/>
                </StackPanel>
            </Border>
            
            <!-- Auto Capture Settings -->
            <Border Style="{StaticResource GlassSectionStyle}">
                <StackPanel>
                    <StackPanel Style="{StaticResource SectionHeaderStyle}">
                        <TextBlock Text="📷" Style="{StaticResource SectionIconStyle}"/>
                        <TextBlock Text="Auto Capture Mode" Style="{StaticResource SectionTitleStyle}"/>
                    </StackPanel>
                    
                    <CheckBox x:Name="autoCaptureCheckBox" 
                              Content="Enable continuous auto-capture mode" 
                              IsChecked="False"
                              Style="{StaticResource SettingsCheckBoxStyle}"/>
                    
                    <TextBlock Text="Delay between captures (seconds)" Style="{StaticResource SettingsTextStyle}"/>
                    <StackPanel Orientation="Horizontal" Margin="0,8,0,20">
                        <Slider x:Name="autoCaptureDelaySlider" 
                                Minimum="3" 
                                Maximum="30" 
                                Value="10" 
                                TickPlacement="BottomRight" 
                                TickFrequency="5" 
                                IsSnapToTickEnabled="True"
                                Width="300"
                                ValueChanged="AutoCaptureDelaySlider_ValueChanged"
                                Style="{StaticResource SettingsSliderStyle}"/>
                        <TextBlock x:Name="autoCaptureDelayValueText" 
                                   Text="10 seconds" 
                                   VerticalAlignment="Center" 
                                   Margin="20,0,0,0" 
                                   Foreground="White" 
                                   FontWeight="Bold"
                                   FontSize="18"/>
                    </StackPanel>
                </StackPanel>
            </Border>
            
            <!-- Photo Storage Settings -->
            <Border Style="{StaticResource GlassSectionStyle}">
                <StackPanel>
                    <StackPanel Style="{StaticResource SectionHeaderStyle}">
                        <TextBlock Text="💾" Style="{StaticResource SectionIconStyle}"/>
                        <TextBlock Text="Photo Storage" Style="{StaticResource SectionTitleStyle}"/>
                    </StackPanel>
                    
                    <TextBlock Text="Photos save location:" Style="{StaticResource SettingsTextStyle}"/>
                    <StackPanel Orientation="Horizontal" Margin="0,8,0,20">
                        <TextBox x:Name="photoLocationTextBox" 
                                 Text="C:\Users\...\Pictures\Photobooth" 
                                 Width="450"
                                 Style="{StaticResource SettingsTextBoxStyle}"/>
                        <Button Content="📁 Browse" 
                                Click="BrowsePhotoLocation_Click"
                                Margin="15,0,0,0" 
                                Padding="20,12"
                                Style="{StaticResource ModernButtonStyle}">
                            <Button.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#48BB78" Offset="0"/>
                                    <GradientStop Color="#38A169" Offset="1"/>
                                </LinearGradientBrush>
                            </Button.Background>
                        </Button>
                    </StackPanel>
                    
                    <TextBlock Text="Photo filename format:" Style="{StaticResource SettingsTextStyle}"/>
                    <ComboBox x:Name="filenameFormatComboBox" 
                              Style="{StaticResource SettingsComboBoxStyle}"
                              Width="450"
                              HorizontalAlignment="Left">
                        <ComboBoxItem Content="Photobooth_YYYYMMDD_HHMMSS" IsSelected="True"/>
                        <ComboBoxItem Content="Photo_YYYYMMDD_HHMMSS"/>
                        <ComboBoxItem Content="IMG_YYYYMMDD_HHMMSS"/>
                        <ComboBoxItem Content="Booth_Counter"/>
                    </ComboBox>
                </StackPanel>
            </Border>
            
            <!-- Live View Settings -->
            <Border Style="{StaticResource GlassSectionStyle}">
                <StackPanel>
                    <StackPanel Style="{StaticResource SectionHeaderStyle}">
                        <TextBlock Text="👁️" Style="{StaticResource SectionIconStyle}"/>
                        <TextBlock Text="Live View Settings" Style="{StaticResource SectionTitleStyle}"/>
                    </StackPanel>
                    
                    <CheckBox x:Name="mirrorLiveViewCheckBox" 
                              Content="Mirror live view (for front-facing setup)" 
                              Style="{StaticResource SettingsCheckBoxStyle}"/>
                    
                    <TextBlock Text="Live view frame rate (FPS)" Style="{StaticResource SettingsTextStyle}"/>
                    <StackPanel Orientation="Horizontal" Margin="0,8,0,20">
                        <Slider x:Name="frameRateSlider" 
                                Minimum="15" 
                                Maximum="30" 
                                Value="30" 
                                TickPlacement="BottomRight" 
                                TickFrequency="5" 
                                IsSnapToTickEnabled="True"
                                Width="300"
                                ValueChanged="FrameRateSlider_ValueChanged"
                                Style="{StaticResource SettingsSliderStyle}"/>
                        <TextBlock x:Name="frameRateValueText" 
                                   Text="30 FPS" 
                                   VerticalAlignment="Center" 
                                   Margin="20,0,0,0" 
                                   Foreground="White" 
                                   FontWeight="Bold"
                                   FontSize="18"/>
                    </StackPanel>
                </StackPanel>
            </Border>
            
            <!-- Retake Settings -->
            <Border Style="{StaticResource GlassSectionStyle}">
                <StackPanel>
                    <StackPanel Style="{StaticResource SectionHeaderStyle}">
                        <TextBlock Text="🔄" Style="{StaticResource SectionIconStyle}"/>
                        <TextBlock Text="Photo Retake Options" Style="{StaticResource SectionTitleStyle}"/>
                    </StackPanel>
                    
                    <CheckBox x:Name="enableRetakeCheckBox" 
                              Content="Enable photo retake option after capture" 
                              IsChecked="False"
                              Style="{StaticResource SettingsCheckBoxStyle}"
                              ToolTip="When enabled, users can review and retake individual photos before finalizing the session"/>
                    
                    <TextBlock Text="Retake review timeout (seconds)" Style="{StaticResource SettingsTextStyle}"/>
                    <StackPanel Orientation="Horizontal" Margin="0,8,0,20">
                        <Slider x:Name="retakeTimeoutSlider" 
                                Minimum="5" 
                                Maximum="30" 
                                Value="15" 
                                TickPlacement="BottomRight" 
                                TickFrequency="5" 
                                IsSnapToTickEnabled="True"
                                Width="300"
                                ValueChanged="RetakeTimeoutSlider_ValueChanged"
                                Style="{StaticResource SettingsSliderStyle}"
                                IsEnabled="{Binding IsChecked, ElementName=enableRetakeCheckBox}"/>
                        <TextBlock x:Name="retakeTimeoutValueText" 
                                   Text="15 seconds" 
                                   VerticalAlignment="Center" 
                                   Margin="20,0,0,0" 
                                   Foreground="White" 
                                   FontWeight="Bold"
                                   FontSize="18"/>
                    </StackPanel>
                    
                    <CheckBox x:Name="allowMultipleRetakesCheckBox" 
                              Content="Allow multiple retakes of the same photo" 
                              IsChecked="True"
                              Style="{StaticResource SettingsCheckBoxStyle}"
                              IsEnabled="{Binding IsChecked, ElementName=enableRetakeCheckBox}"/>
                </StackPanel>
            </Border>
            
            <!-- Photo Filters Settings -->
            <Border Style="{StaticResource GlassSectionStyle}">
                <StackPanel>
                    <StackPanel Style="{StaticResource SectionHeaderStyle}">
                        <TextBlock Text="🎨" Style="{StaticResource SectionIconStyle}"/>
                        <TextBlock Text="Photo Filters" Style="{StaticResource SectionTitleStyle}"/>
                    </StackPanel>
                    
                    <CheckBox x:Name="enableFiltersCheckBox" 
                              Content="Enable photo filters" 
                              IsChecked="True"
                              Style="{StaticResource SettingsCheckBoxStyle}"
                              ToolTip="Allow users to apply filters to their photos"/>
                    
                    <TextBlock Text="Default filter:" Style="{StaticResource SettingsTextStyle}"
                               IsEnabled="{Binding IsChecked, ElementName=enableFiltersCheckBox}"/>
                    <ComboBox x:Name="defaultFilterComboBox" 
                              Style="{StaticResource SettingsComboBoxStyle}"
                              Width="300"
                              HorizontalAlignment="Left"
                              IsEnabled="{Binding IsChecked, ElementName=enableFiltersCheckBox}">
                        <ComboBoxItem Content="None" IsSelected="True"/>
                        <ComboBoxItem Content="Black &amp; White"/>
                        <ComboBoxItem Content="Sepia"/>
                        <ComboBoxItem Content="Vintage"/>
                        <ComboBoxItem Content="Glamour"/>
                        <ComboBoxItem Content="Cool"/>
                        <ComboBoxItem Content="Warm"/>
                        <ComboBoxItem Content="High Contrast"/>
                        <ComboBoxItem Content="Soft Focus"/>
                        <ComboBoxItem Content="Vivid"/>
                    </ComboBox>
                    
                    <TextBlock Text="Filter intensity:" Style="{StaticResource SettingsTextStyle}"
                               IsEnabled="{Binding IsChecked, ElementName=enableFiltersCheckBox}"/>
                    <StackPanel Orientation="Horizontal" Margin="0,8,0,20">
                        <Slider x:Name="filterIntensitySlider" 
                                Minimum="0" 
                                Maximum="100" 
                                Value="100" 
                                TickPlacement="BottomRight" 
                                TickFrequency="25" 
                                IsSnapToTickEnabled="True"
                                Width="300"
                                ValueChanged="FilterIntensitySlider_ValueChanged"
                                Style="{StaticResource SettingsSliderStyle}"
                                IsEnabled="{Binding IsChecked, ElementName=enableFiltersCheckBox}"/>
                        <TextBlock x:Name="filterIntensityValueText" 
                                   Text="100%" 
                                   VerticalAlignment="Center" 
                                   Margin="20,0,0,0" 
                                   Foreground="White" 
                                   FontWeight="Bold"
                                   FontSize="18"/>
                    </StackPanel>
                    
                    <CheckBox x:Name="allowFilterChangeCheckBox" 
                              Content="Allow users to change filters during session" 
                              IsChecked="True"
                              Style="{StaticResource SettingsCheckBoxStyle}"
                              IsEnabled="{Binding IsChecked, ElementName=enableFiltersCheckBox}"/>
                    
                    <CheckBox x:Name="showFilterPreviewCheckBox" 
                              Content="Show live filter preview" 
                              IsChecked="True"
                              Style="{StaticResource SettingsCheckBoxStyle}"
                              IsEnabled="{Binding IsChecked, ElementName=enableFiltersCheckBox}"/>
                    
                    <!-- Individual Filter Selection -->
                    <Border Background="Transparent" 
                            BorderBrush="#4A5568" 
                            BorderThickness="1" 
                            CornerRadius="15" 
                            Padding="20" 
                            Margin="0,20,0,20">
                        <Border.Effect>
                            <DropShadowEffect Color="Black" BlurRadius="8" ShadowDepth="4" Opacity="0.2"/>
                        </Border.Effect>
                        <StackPanel IsEnabled="{Binding IsChecked, ElementName=enableFiltersCheckBox}">
                            <TextBlock Text="📋 Available Filters:" 
                                       Style="{StaticResource SettingsTextStyle}"
                                       FontWeight="Bold"
                                       FontSize="18"
                                       Margin="0,0,0,15"/>
                            
                            <Border Background="#2D3748" 
                                    CornerRadius="12" 
                                    Padding="15" 
                                    Margin="0,0,0,15"
                                    MaxHeight="300">
                                <Border.Effect>
                                    <DropShadowEffect Color="Black" BlurRadius="4" ShadowDepth="2" Opacity="0.3"/>
                                </Border.Effect>
                                <ScrollViewer VerticalScrollBarVisibility="Auto">
                                    <StackPanel x:Name="filterListPanel">
                                        <!-- Filter checkboxes with reduced margins for compact display -->
                                        <CheckBox x:Name="filterBlackWhiteCheckBox" 
                                                  Content="⚫ Black &amp; White" 
                                                  IsChecked="True"
                                                  Style="{StaticResource SettingsCheckBoxStyle}"
                                                  Margin="8,5"/>
                                        <CheckBox x:Name="filterSepiaCheckBox" 
                                                  Content="🟤 Sepia" 
                                                  IsChecked="True"
                                                  Style="{StaticResource SettingsCheckBoxStyle}"
                                                  Margin="8,5"/>
                                        <CheckBox x:Name="filterVintageCheckBox" 
                                                  Content="📸 Vintage" 
                                                  IsChecked="True"
                                                  Style="{StaticResource SettingsCheckBoxStyle}"
                                                  Margin="8,5"/>
                                        <CheckBox x:Name="filterGlamourCheckBox" 
                                                  Content="✨ Glamour" 
                                                  IsChecked="True"
                                                  Style="{StaticResource SettingsCheckBoxStyle}"
                                                  Margin="8,5"/>
                                        <CheckBox x:Name="filterCoolCheckBox" 
                                                  Content="🧊 Cool" 
                                                  IsChecked="True"
                                                  Style="{StaticResource SettingsCheckBoxStyle}"
                                                  Margin="8,5"/>
                                        <CheckBox x:Name="filterWarmCheckBox" 
                                                  Content="🔥 Warm" 
                                                  IsChecked="True"
                                                  Style="{StaticResource SettingsCheckBoxStyle}"
                                                  Margin="8,5"/>
                                        <CheckBox x:Name="filterHighContrastCheckBox" 
                                                  Content="⚡ High Contrast" 
                                                  IsChecked="True"
                                                  Style="{StaticResource SettingsCheckBoxStyle}"
                                                  Margin="8,5"/>
                                        <CheckBox x:Name="filterSoftCheckBox" 
                                                  Content="☁️ Soft Focus" 
                                                  IsChecked="True"
                                                  Style="{StaticResource SettingsCheckBoxStyle}"
                                                  Margin="8,5"/>
                                        <CheckBox x:Name="filterVividCheckBox" 
                                                  Content="🌈 Vivid" 
                                                  IsChecked="True"
                                                  Style="{StaticResource SettingsCheckBoxStyle}"
                                                  Margin="8,5"/>
                                    </StackPanel>
                                </ScrollViewer>
                            </Border>
                            
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Content="✅ Select All" 
                                        Width="140"
                                        Margin="0,0,15,0"
                                        Click="SelectAllFilters_Click"
                                        Style="{StaticResource ModernButtonStyle}">
                                    <Button.Background>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#48BB78" Offset="0"/>
                                            <GradientStop Color="#38A169" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Button.Background>
                                </Button>
                                <Button Content="❌ Deselect All" 
                                        Width="140"
                                        Click="DeselectAllFilters_Click"
                                        Style="{StaticResource ModernButtonStyle}">
                                    <Button.Background>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#F56565" Offset="0"/>
                                            <GradientStop Color="#E53E3E" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Button.Background>
                                </Button>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>
            
            <!-- Touch Interface Settings -->
            <Border Style="{StaticResource GlassSectionStyle}">
                <StackPanel>
                    <StackPanel Style="{StaticResource SectionHeaderStyle}">
                        <TextBlock Text="👆" Style="{StaticResource SectionIconStyle}"/>
                        <TextBlock Text="Touch Interface" Style="{StaticResource SectionTitleStyle}"/>
                    </StackPanel>
                    
                    <CheckBox x:Name="fullscreenCheckBox" 
                              Content="Launch in fullscreen mode" 
                              Style="{StaticResource SettingsCheckBoxStyle}"/>
                    
                    <CheckBox x:Name="hideCursorCheckBox" 
                              Content="Hide mouse cursor during operation" 
                              Style="{StaticResource SettingsCheckBoxStyle}"/>
                    
                    <TextBlock Text="Button size scaling" Style="{StaticResource SettingsTextStyle}"/>
                    <StackPanel Orientation="Horizontal" Margin="0,8,0,20">
                        <Slider x:Name="buttonSizeSlider" 
                                Minimum="0.8" 
                                Maximum="1.5" 
                                Value="1.0" 
                                TickPlacement="BottomRight" 
                                TickFrequency="0.1" 
                                IsSnapToTickEnabled="True"
                                Width="300"
                                ValueChanged="ButtonSizeSlider_ValueChanged"
                                Style="{StaticResource SettingsSliderStyle}"/>
                        <TextBlock x:Name="buttonSizeValueText" 
                                   Text="100%" 
                                   VerticalAlignment="Center" 
                                   Margin="20,0,0,0" 
                                   Foreground="White" 
                                   FontWeight="Bold"
                                   FontSize="18"/>
                    </StackPanel>
                    
                    <!-- Print Settings within Touch Interface -->
                    <Border Background="Transparent" 
                            BorderBrush="#4A5568" 
                            BorderThickness="1" 
                            CornerRadius="20" 
                            Padding="25" 
                            Margin="0,25,0,0">
                        <Border.Effect>
                            <DropShadowEffect Color="Black" BlurRadius="8" ShadowDepth="4" Opacity="0.2"/>
                        </Border.Effect>
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                                <TextBlock Text="🖨️" 
                                           FontSize="24" 
                                           Foreground="#667EEA" 
                                           Margin="0,0,15,0" 
                                           VerticalAlignment="Center"/>
                                <TextBlock Text="Print Settings" 
                                           Style="{StaticResource SectionTitleStyle}"
                                           FontSize="20"/>
                            </StackPanel>
                            
                            <CheckBox x:Name="enablePrintingCheckBox" 
                                      Content="Enable printing functionality" 
                                      Style="{StaticResource SettingsCheckBoxStyle}"
                                      IsChecked="True"/>
                            
                            <CheckBox x:Name="showPrintButtonCheckBox" 
                                      Content="Show print button in gallery" 
                                      Style="{StaticResource SettingsCheckBoxStyle}"
                                      IsChecked="True"
                                      IsEnabled="{Binding IsChecked, ElementName=enablePrintingCheckBox}"/>
                            
                            <CheckBox x:Name="printOnlyOriginalsCheckBox" 
                                      Content="Print only original photos (no filtered/templates)" 
                                      Style="{StaticResource SettingsCheckBoxStyle}"
                                      IsChecked="True"
                                      IsEnabled="{Binding IsChecked, ElementName=enablePrintingCheckBox}"/>
                            
                            <CheckBox x:Name="allowReprintsCheckBox" 
                                      Content="Allow reprints of same photos" 
                                      Style="{StaticResource SettingsCheckBoxStyle}"
                                      IsChecked="True"
                                      IsEnabled="{Binding IsChecked, ElementName=enablePrintingCheckBox}"/>
                            
                            <CheckBox x:Name="showPrintDialogCheckBox" 
                                      Content="Show print dialog before printing" 
                                      Style="{StaticResource SettingsCheckBoxStyle}"
                                      IsChecked="False"
                                      IsEnabled="{Binding IsChecked, ElementName=enablePrintingCheckBox}"/>
                            
                            <!-- Automatic Size-Based Routing -->
                            <CheckBox x:Name="autoRoutePrinterCheckBox" 
                              Content="Automatically route 2x6 strips to dedicated strip printer" 
                              Style="{StaticResource SettingsCheckBoxStyle}"
                              IsChecked="True"
                              IsEnabled="{Binding IsChecked, ElementName=enablePrintingCheckBox}"
                              ToolTip="2x6 strips go to Strip Printer, all other sizes go to Default Printer"
                              Margin="0,10,0,10"/>
                    
                    <!-- Default Printer Selection (for all standard photo sizes) -->
                    <Border Background="#3A3A3A" CornerRadius="5" Padding="10" Margin="0,5">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Default Printer (4x6, 5x7, 8x10, etc.)" 
                                           Style="{StaticResource SettingsTextStyle}"
                                           FontWeight="Bold"
                                           FontSize="14"
                                           Foreground="#FFD700"/>
                                <CheckBox x:Name="defaultPoolingCheckBox" 
                                          Content="Enable Pooling" 
                                          Margin="20,0,0,0"
                                          Foreground="#4CAF50"
                                          FontWeight="Bold"
                                          IsEnabled="{Binding IsChecked, ElementName=enablePrintingCheckBox}"
                                          ToolTip="Enable printer pooling for high-volume printing"
                                          Click="DefaultPooling_Changed"/>
                            </StackPanel>
                            
                            <!-- Primary Printer -->
                            <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                <TextBlock Text="Primary:" 
                                           Width="80"
                                           VerticalAlignment="Center"
                                           Foreground="White"/>
                                <ComboBox x:Name="printer4x6ComboBox" 
                                          Width="200"
                                          Height="30"
                                          IsEnabled="{Binding IsChecked, ElementName=enablePrintingCheckBox}"
                                          ToolTip="Select primary printer for standard photos"/>
                                <Button Content="🔄" 
                                        Width="48"
                                        Height="48"
                                        Margin="10,0,0,0"
                                        FontSize="14"
                                        Click="RefreshPrinters_Click"
                                        IsEnabled="{Binding IsChecked, ElementName=enablePrintingCheckBox}"
                                        Style="{StaticResource ModernButtonStyle}"
                                        ToolTip="Refresh printer list">
                                    <Button.Background>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#718096" Offset="0"/>
                                            <GradientStop Color="#4A5568" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Button.Background>
                                </Button>
                                <Button Content="🧪" 
                                        Width="48"
                                        Height="48"
                                        Margin="8,0,0,0"
                                        FontSize="14"
                                        Click="Test4x6Print_Click"
                                        ToolTip="Print a test page using paper size from settings"
                                        IsEnabled="{Binding IsChecked, ElementName=enablePrintingCheckBox}"
                                        Style="{StaticResource ModernButtonStyle}">
                                    <Button.Background>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#ECC94B" Offset="0"/>
                                            <GradientStop Color="#D69E2E" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Button.Background>
                                </Button>
                                <Button Content="⚙️" 
                                        Width="48"
                                        Height="48"
                                        Margin="8,0,0,0"
                                        FontSize="14"
                                        Click="AdvancedDriver4x6_Click"
                                        ToolTip="Configure default printer driver settings"
                                        IsEnabled="{Binding IsChecked, ElementName=enablePrintingCheckBox}"
                                        Style="{StaticResource ModernButtonStyle}">
                                    <Button.Background>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#48BB78" Offset="0"/>
                                            <GradientStop Color="#38A169" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Button.Background>
                                </Button>
                            </StackPanel>
                            
                            <!-- Printer Pool (shown when pooling is enabled) -->
                            <StackPanel x:Name="defaultPoolPanel" Visibility="Collapsed">
                                <Separator Margin="0,5" Background="#555"/>
                                
                                <!-- Pool Strategy -->
                                <StackPanel Orientation="Horizontal" Margin="0,5">
                                    <TextBlock Text="Strategy:" 
                                               Width="80"
                                               VerticalAlignment="Center"
                                               Foreground="#AAA"/>
                                    <ComboBox x:Name="defaultPoolStrategyComboBox" 
                                              Width="120"
                                              Height="25"
                                              IsEnabled="{Binding IsChecked, ElementName=enablePrintingCheckBox}">
                                        <ComboBoxItem Content="Round-Robin" IsSelected="True"/>
                                        <ComboBoxItem Content="Load Balance"/>
                                        <ComboBoxItem Content="Failover Only"/>
                                    </ComboBox>
                                    <TextBlock Text="(How to distribute jobs)" 
                                               Margin="10,0,0,0"
                                               VerticalAlignment="Center"
                                               Foreground="#888"
                                               FontSize="11"/>
                                </StackPanel>
                                
                                <!-- Additional Printers -->
                                <TextBlock Text="Additional Pool Printers:" 
                                           Foreground="#AAA"
                                           FontSize="12"
                                           Margin="0,5,0,2"/>
                                
                                <StackPanel Orientation="Horizontal" Margin="0,2">
                                    <CheckBox x:Name="defaultPool2CheckBox" 
                                              Width="20"
                                              IsEnabled="{Binding IsChecked, ElementName=enablePrintingCheckBox}"/>
                                    <TextBlock Text="Printer 2:" 
                                               Width="60"
                                               VerticalAlignment="Center"
                                               Foreground="#AAA"
                                               FontSize="11"/>
                                    <ComboBox x:Name="defaultPool2ComboBox" 
                                              Width="180"
                                              Height="24"
                                              FontSize="11"
                                              IsEnabled="{Binding IsChecked, ElementName=defaultPool2CheckBox}"/>
                                </StackPanel>
                                
                                <StackPanel Orientation="Horizontal" Margin="0,2">
                                    <CheckBox x:Name="defaultPool3CheckBox" 
                                              Width="20"
                                              IsEnabled="{Binding IsChecked, ElementName=enablePrintingCheckBox}"/>
                                    <TextBlock Text="Printer 3:" 
                                               Width="60"
                                               VerticalAlignment="Center"
                                               Foreground="#AAA"
                                               FontSize="11"/>
                                    <ComboBox x:Name="defaultPool3ComboBox" 
                                              Width="180"
                                              Height="24"
                                              FontSize="11"
                                              IsEnabled="{Binding IsChecked, ElementName=defaultPool3CheckBox}"/>
                                </StackPanel>
                                
                                <StackPanel Orientation="Horizontal" Margin="0,2">
                                    <CheckBox x:Name="defaultPool4CheckBox" 
                                              Width="20"
                                              IsEnabled="{Binding IsChecked, ElementName=enablePrintingCheckBox}"/>
                                    <TextBlock Text="Printer 4:" 
                                               Width="60"
                                               VerticalAlignment="Center"
                                               Foreground="#AAA"
                                               FontSize="11"/>
                                    <ComboBox x:Name="defaultPool4ComboBox" 
                                              Width="180"
                                              Height="24"
                                              FontSize="11"
                                              IsEnabled="{Binding IsChecked, ElementName=defaultPool4CheckBox}"/>
                                </StackPanel>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                    
                    <!-- 2x6 Printer Selection -->
                    <Border Background="#3A3A3A" CornerRadius="5" Padding="10" Margin="0,5">
                        <StackPanel>
                            <TextBlock Text="2x6 Strip Printer (with 2-inch cut)" 
                                       Style="{StaticResource SettingsTextStyle}"
                                       FontWeight="Bold"
                                       FontSize="14"
                                       Foreground="#00BCD4"
                                       Margin="0,0,0,5"/>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Printer:" 
                                           Width="80"
                                           VerticalAlignment="Center"
                                           Foreground="White"/>
                                <ComboBox x:Name="printer2x6ComboBox" 
                                          Width="200"
                                          Height="30"
                                          IsEnabled="{Binding IsChecked, ElementName=enablePrintingCheckBox}"
                                          ToolTip="Select printer for 2x6 photo strips"/>
                                <Button Content="🔄" 
                                        Width="48"
                                        Height="48"
                                        Margin="10,0,0,0"
                                        FontSize="14"
                                        Click="RefreshPrinters_Click"
                                        IsEnabled="{Binding IsChecked, ElementName=enablePrintingCheckBox}"
                                        Style="{StaticResource ModernButtonStyle}"
                                        ToolTip="Refresh printer list">
                                    <Button.Background>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#718096" Offset="0"/>
                                            <GradientStop Color="#4A5568" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Button.Background>
                                </Button>
                                <Button Content="📏" 
                                        Width="48"
                                        Height="48"
                                        Margin="8,0,0,0"
                                        FontSize="14"
                                        Click="Test2x6Print_Click"
                                        ToolTip="Print a test 2x6 strip"
                                        IsEnabled="{Binding IsChecked, ElementName=enablePrintingCheckBox}"
                                        Style="{StaticResource ModernButtonStyle}">
                                    <Button.Background>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#00BCD4" Offset="0"/>
                                            <GradientStop Color="#0097A7" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Button.Background>
                                </Button>
                                <Button Content="⚙️" 
                                        Width="48"
                                        Height="48"
                                        Margin="8,0,0,0"
                                        FontSize="14"
                                        Click="AdvancedDriver2x6_Click"
                                        ToolTip="Configure 2x6 printer driver settings (enable 2-inch cut here)"
                                        IsEnabled="{Binding IsChecked, ElementName=enablePrintingCheckBox}"
                                        Style="{StaticResource ModernButtonStyle}">
                                    <Button.Background>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#48BB78" Offset="0"/>
                                            <GradientStop Color="#38A169" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Button.Background>
                                </Button>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                    
                    <!-- Legacy single printer (hidden) -->
                    <StackPanel x:Name="legacyPrinterPanel" Visibility="Collapsed">
                        <ComboBox x:Name="printerComboBox" Width="200" Height="30"/>
                    </StackPanel>
                    
                    <StackPanel Orientation="Horizontal" Margin="0,5">
                        <TextBlock Text="Paper Size:" 
                                   Width="120"
                                   VerticalAlignment="Center"
                                   Foreground="White"/>
                        <ComboBox x:Name="paperSizeComboBox" 
                                  Width="150"
                                  Height="30"
                                  IsEnabled="{Binding IsChecked, ElementName=enablePrintingCheckBox}">
                            <ComboBoxItem Content="4x6" IsSelected="True"/>
                            <ComboBoxItem Content="5x7"/>
                            <ComboBoxItem Content="8x10"/>
                            <ComboBoxItem Content="Letter"/>
                            <ComboBoxItem Content="A4"/>
                        </ComboBox>
                    </StackPanel>
                    
                    <CheckBox x:Name="printLandscapeCheckBox" 
                              Content="Print in landscape orientation" 
                              Style="{StaticResource SettingsCheckBoxStyle}"
                              IsChecked="False"
                              IsEnabled="{Binding IsChecked, ElementName=enablePrintingCheckBox}"/>
                    
                    <StackPanel Orientation="Horizontal" Margin="0,10,0,5">
                        <TextBlock Text="Max prints per session:" 
                                   Width="150"
                                   VerticalAlignment="Center"
                                   Foreground="White"/>
                        <TextBox x:Name="maxSessionPrintsTextBox" 
                                 Width="60"
                                 Height="25"
                                 Text="5"
                                 Background="#5A5A5A"
                                 Foreground="White"
                                 BorderBrush="#666666"
                                 IsEnabled="{Binding IsChecked, ElementName=enablePrintingCheckBox}"/>
                        <TextBlock Text="(0 = unlimited)" 
                                   Margin="10,0,0,0"
                                   VerticalAlignment="Center"
                                   Foreground="#AAAAAA"
                                   FontSize="12"/>
                    </StackPanel>
                    
                    <StackPanel Orientation="Horizontal" Margin="0,5">
                        <TextBlock Text="Max prints per event:" 
                                   Width="150"
                                   VerticalAlignment="Center"
                                   Foreground="White"/>
                        <TextBox x:Name="maxEventPrintsTextBox" 
                                 Width="60"
                                 Height="25"
                                 Text="100"
                                 Background="#5A5A5A"
                                 Foreground="White"
                                 BorderBrush="#666666"
                                 IsEnabled="{Binding IsChecked, ElementName=enablePrintingCheckBox}"/>
                        <TextBlock Text="(0 = unlimited)" 
                                   Margin="10,0,0,0"
                                   VerticalAlignment="Center"
                                   Foreground="#AAAAAA"
                                   FontSize="12"/>
                    </StackPanel>
                    
                    <StackPanel Orientation="Horizontal" Margin="0,5">
                        <TextBlock Text="Default copies:" 
                                   Width="150"
                                   VerticalAlignment="Center"
                                   Foreground="White"/>
                        <TextBox x:Name="defaultCopiesTextBox" 
                                 Width="60"
                                 Height="25"
                                 Text="1"
                                 Background="#5A5A5A"
                                 Foreground="White"
                                 BorderBrush="#666666"
                                 IsEnabled="{Binding IsChecked, ElementName=enablePrintingCheckBox}"/>
                    </StackPanel>
                    
                </StackPanel>
            </Border>
            </StackPanel>
            </Border>
            
            
            <!-- Action Buttons -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,40,0,40">
                <Button Content="💾 Save Settings" 
                        Click="SaveSettings_Click"
                        Style="{StaticResource SaveButtonStyle}"/>
                
                <Button Content="🔄 Reset to Defaults" 
                        Click="ResetSettings_Click"
                        Style="{StaticResource ResetButtonStyle}"/>
                
                <Button Content="🚀 Open Photobooth" 
                        Click="OpenPhotobooth_Click"
                        Style="{StaticResource OpenButtonStyle}"/>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</UserControl>