<Page x:Class="Photobooth.Pages.EventSelectionPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Photobooth.Pages"
      mc:Ignorable="d" 
      Background="#0A0E27"
      Title="Select Event"
      UseLayoutRounding="True"
      SnapsToDevicePixels="True"
      RenderOptions.BitmapScalingMode="HighQuality"
      TextOptions.TextFormattingMode="Display"
      FontFamily="Segoe UI">

    <Page.Resources>
        <!-- Colors to match main theme -->
        <Color x:Key="PrimaryColor">#0A0E27</Color>
        <Color x:Key="SecondaryColor">#1A1E37</Color>
        <Color x:Key="AccentColor">#00D9FF</Color>
        <Color x:Key="AccentPurple">#7928CA</Color>
        <Color x:Key="AccentPink">#FF0080</Color>
        
        <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource PrimaryColor}"/>
        <SolidColorBrush x:Key="SecondaryBrush" Color="{StaticResource SecondaryColor}"/>
        <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}"/>

        <!-- Gradient Brushes -->
        <LinearGradientBrush x:Key="AccentGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="{StaticResource AccentPink}" Offset="0"/>
            <GradientStop Color="{StaticResource AccentPurple}" Offset="0.5"/>
            <GradientStop Color="{StaticResource AccentColor}" Offset="1"/>
        </LinearGradientBrush>

        <!-- Event Card Style -->
        <Style x:Key="EventCardStyle" TargetType="Border">
            <Setter Property="Width" Value="350"/>
            <Setter Property="Height" Value="200"/>
            <Setter Property="Margin" Value="20"/>
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#1A1E37" Offset="0"/>
                        <GradientStop Color="#2A2E47" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="CornerRadius" Value="20"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" BlurRadius="20" ShadowDepth="5" Opacity="0.3"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect Color="#7928CA" BlurRadius="30" ShadowDepth="0" Opacity="0.5"/>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="RenderTransform">
                        <Setter.Value>
                            <TranslateTransform Y="-5"/>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Action Button Style -->
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="MinWidth" Value="200"/>
            <Setter Property="Height" Value="60"/>
            <Setter Property="Background" Value="{StaticResource AccentGradient}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}" 
                                CornerRadius="30">
                            <Border.Effect>
                                <DropShadowEffect Color="#7928CA" 
                                                  BlurRadius="15" 
                                                  ShadowDepth="2" 
                                                  Opacity="0.4"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.95" ScaleY="0.95"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Search Box Style -->
        <Style x:Key="SearchBoxStyle" TargetType="TextBox">
            <Setter Property="Height" Value="50"/>
            <Setter Property="Background" Value="#1A1E37"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#2A2E47"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="25">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="🔍" 
                                           FontSize="20" 
                                           Margin="15,0,5,0"
                                           VerticalAlignment="Center"
                                           Foreground="#666"/>
                                <ScrollViewer Grid.Column="1" x:Name="PART_ContentHost" 
                                              Margin="5,0,15,0"
                                              VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid>
        <!-- Background Gradient -->
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#0A0E27" Offset="0"/>
                <GradientStop Color="#0F1329" Offset="0.5"/>
                <GradientStop Color="#1A1E37" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>  <!-- Header -->
            <RowDefinition Height="Auto"/>  <!-- Search -->
            <RowDefinition Height="*"/>     <!-- Events List -->
            <RowDefinition Height="Auto"/>  <!-- Action Buttons -->
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="40,30,40,20">
            <TextBlock Text="SELECT EVENT" 
                       FontSize="36" 
                       FontWeight="Light"
                       Foreground="White"
                       HorizontalAlignment="Center"/>
            <TextBlock Text="Choose an event to start the photo booth session" 
                       FontSize="18" 
                       Foreground="#B8BCC8"
                       HorizontalAlignment="Center"
                       Margin="0,10,0,0"/>
        </StackPanel>

        <!-- Search Bar -->
        <Grid Grid.Row="1" Margin="40,0,40,20">
            <TextBox x:Name="SearchBox"
                     Style="{StaticResource SearchBoxStyle}"
                     MaxWidth="600"
                     TextChanged="SearchBox_TextChanged">
                <TextBox.Resources>
                    <Style TargetType="TextBox">
                        <Style.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter Property="Tag" Value="Search events..."/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </TextBox.Resources>
            </TextBox>
        </Grid>

        <!-- Events ScrollViewer -->
        <ScrollViewer Grid.Row="2" 
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled"
                      Margin="20,0">
            <ScrollViewer.Resources>
                <Style TargetType="ScrollBar">
                    <Setter Property="Width" Value="12"/>
                    <Setter Property="Background" Value="#1A1E37"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ScrollBar">
                                <Grid Width="12">
                                    <Border Background="#1A1E37" CornerRadius="6"/>
                                    <Track x:Name="PART_Track" IsDirectionReversed="True">
                                        <Track.Thumb>
                                            <Thumb>
                                                <Thumb.Template>
                                                    <ControlTemplate TargetType="Thumb">
                                                        <Border Background="{StaticResource AccentGradient}" 
                                                                CornerRadius="6" 
                                                                Margin="2"/>
                                                    </ControlTemplate>
                                                </Thumb.Template>
                                            </Thumb>
                                        </Track.Thumb>
                                    </Track>
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ScrollViewer.Resources>

            <WrapPanel x:Name="EventsPanel" 
                       HorizontalAlignment="Center"
                       Margin="0,0,0,20">
                <!-- Event cards will be added here dynamically -->
            </WrapPanel>
        </ScrollViewer>

        <!-- Empty State -->
        <Grid x:Name="EmptyState" Grid.Row="2" 
              Visibility="Collapsed">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="📅" FontSize="64" 
                           HorizontalAlignment="Center"
                           Foreground="#666"/>
                <TextBlock Text="No Events Found" 
                           FontSize="24" 
                           Foreground="#999"
                           Margin="0,20,0,10"/>
                <TextBlock Text="Create a new event to get started" 
                           FontSize="16" 
                           Foreground="#666"
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>

        <!-- Action Buttons Bar -->
        <Border Grid.Row="3" 
                Background="{StaticResource SecondaryBrush}"
                BorderBrush="#2A2E47" 
                BorderThickness="0,1,0,0">
            <Grid Height="100">
                <StackPanel Orientation="Horizontal" 
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center">
                    
                    <!-- Create New Event Button -->
                    <Button x:Name="CreateEventButton"
                            Style="{StaticResource ActionButtonStyle}"
                            Click="CreateEvent_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="➕" FontSize="20" Margin="0,0,10,0"/>
                            <TextBlock Text="Create Event" FontSize="16"/>
                        </StackPanel>
                    </Button>

                    <!-- Quick Start Button (No Event) -->
                    <Button x:Name="QuickStartButton"
                            Style="{StaticResource ActionButtonStyle}"
                            Background="#2A2E47"
                            Click="QuickStart_Click">
                        <Button.Effect>
                            <DropShadowEffect Color="Black" 
                                              BlurRadius="10" 
                                              ShadowDepth="2" 
                                              Opacity="0.3"/>
                        </Button.Effect>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="⚡" FontSize="20" Margin="0,0,10,0"/>
                            <TextBlock Text="Quick Start" FontSize="16"/>
                        </StackPanel>
                    </Button>

                    <!-- Cancel Button -->
                    <Button x:Name="CancelButton"
                            Width="120" Height="50"
                            Background="#E74C3C"
                            Click="Cancel_Click"
                            Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                        CornerRadius="25">
                                    <Border.Effect>
                                        <DropShadowEffect Color="#E74C3C" 
                                                          BlurRadius="10" 
                                                          ShadowDepth="0" 
                                                          Opacity="0.3"/>
                                    </Border.Effect>
                                    <TextBlock Text="Cancel" 
                                               FontSize="16" 
                                               FontWeight="SemiBold"
                                               Foreground="White"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#C0392B"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Page>