<Window x:Class="Photobooth.WelcomeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Photobooth"
        mc:Ignorable="d"
        Title="WelcomeWindow" Height="450" Width="800" Background="#333333" Icon="images/photobooth.png" WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <ControlTemplate x:Key="NoMouseOverButtonTemplate"   
                 TargetType="Button">
            <Border Background="{TemplateBinding Background}"  
          BorderBrush="{TemplateBinding BorderBrush}"  
          BorderThickness="{TemplateBinding BorderThickness}">
                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"  
                      VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
            </Border>

            <ControlTemplate.Triggers>
                <Trigger Property="IsEnabled"  
             Value="False">
                    <Setter Property="Background"  
              Value="{x:Static SystemColors.ControlLightBrush}" />
                    <Setter Property="Foreground"  
              Value="{x:Static SystemColors.GrayTextBrush}" />
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>
     
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Orientation="Vertical" VerticalAlignment="Center">
            <!--Welcome to the Photobooth!-->
            <TextBlock Text="Welcome to the Photobooth!" FontSize="30" HorizontalAlignment="Center" FontFamily="Copperplate Gothic Bold" Margin="5,0,0,5">
                <TextBlock.Foreground>
                    <RadialGradientBrush>
                        <GradientStop Color="#FFFFFCFC"/>
                        <GradientStop Color="#FFBBBBBB" Offset="1"/>
                    </RadialGradientBrush>
                </TextBlock.Foreground>
            </TextBlock>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Border BorderBrush="#FFFFFF" BorderThickness="1" Width="150" Height="30"  CornerRadius="15" Cursor="Hand">
                    <Border.Style>
                        <Style TargetType="Border">
                          
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#222"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <Button Template="{StaticResource NoMouseOverButtonTemplate}" Content="Start" Width="150" Height="30"  Click="Start_Click" BorderBrush="{x:Null}" Background="{x:Null}" Foreground="White"/>
                </Border>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
