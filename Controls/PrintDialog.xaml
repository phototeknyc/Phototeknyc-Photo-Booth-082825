<Window x:Class="Photobooth.Controls.PrintDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:local="clr-namespace:Photobooth.Controls"
        mc:Ignorable="d" 
        Title="Print Photos" 
        Height="600" 
        Width="800"
        Background="#1E1E1E"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="NoResize">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        
        <Style x:Key="HeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,10,0,20"/>
        </Style>
        
        <Style x:Key="SectionHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#4CAF50"/>
            <Setter Property="Margin" Value="0,15,0,10"/>
        </Style>
        
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,5"/>
        </Style>
        
        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#4CAF50"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#66BB6A"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#388E3C"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#757575"/>
                                <Setter Property="Foreground" Value="#CCCCCC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="CancelButtonStyle" TargetType="Button" BasedOn="{StaticResource ButtonStyle}">
            <Setter Property="Background" Value="#757575"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#9E9E9E"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#2A2A2A" Padding="20">
            <TextBlock Text="Print Photos" Style="{StaticResource HeaderStyle}"/>
        </Border>

        <!-- Main Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Background="#1E1E1E" Padding="20">
            <StackPanel>
                
                <!-- Session Selection -->
                <TextBlock Text="Select Session to Print" Style="{StaticResource SectionHeaderStyle}"/>
                <ComboBox x:Name="sessionComboBox" 
                          Height="35" 
                          Background="#2A2A2A" 
                          Foreground="White" 
                          BorderBrush="#4CAF50"
                          SelectionChanged="SessionComboBox_SelectionChanged"/>
                
                <!-- Print Options -->
                <TextBlock Text="Print Options" Style="{StaticResource SectionHeaderStyle}"/>
                
                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <TextBlock Text="Number of copies:" Style="{StaticResource LabelStyle}" Width="150"/>
                    <TextBox x:Name="copiesTextBox" 
                             Text="1" 
                             Width="80" 
                             Height="25" 
                             Background="#2A2A2A" 
                             Foreground="White" 
                             BorderBrush="#4CAF50"
                             TextAlignment="Center"/>
                </StackPanel>
                
                <CheckBox x:Name="printOnlyOriginalsCheckBox" 
                          Content="Print only original photos (exclude filtered/templates)" 
                          Foreground="White" 
                          IsChecked="True"
                          Margin="0,10"
                          Checked="PrintOnlyOriginalsCheckBox_CheckedChanged"
                          Unchecked="PrintOnlyOriginalsCheckBox_Unchecked"/>
                
                <!-- Print Limits Display -->
                <Border Background="#2A2A2A" 
                        CornerRadius="5" 
                        Padding="15" 
                        Margin="0,10">
                    <StackPanel>
                        <TextBlock Text="Print Limits" Style="{StaticResource SectionHeaderStyle}" Margin="0,0,0,10"/>
                        
                        <StackPanel Orientation="Horizontal" Margin="0,2">
                            <TextBlock Text="Session limit:" Style="{StaticResource LabelStyle}" Width="120"/>
                            <TextBlock x:Name="sessionLimitText" 
                                       Text="5 remaining" 
                                       Foreground="#4CAF50" 
                                       FontWeight="Bold"/>
                        </StackPanel>
                        
                        <StackPanel Orientation="Horizontal" Margin="0,2">
                            <TextBlock Text="Event limit:" Style="{StaticResource LabelStyle}" Width="120"/>
                            <TextBlock x:Name="eventLimitText" 
                                       Text="95 remaining" 
                                       Foreground="#4CAF50" 
                                       FontWeight="Bold"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
                
                <!-- Photo Preview (if session selected) -->
                <TextBlock x:Name="photoPreviewHeader" 
                           Text="Photos to Print" 
                           Style="{StaticResource SectionHeaderStyle}"
                           Visibility="Collapsed"/>
                
                <ScrollViewer x:Name="photoPreviewScrollViewer" 
                              Height="200" 
                              VerticalScrollBarVisibility="Auto"
                              Visibility="Collapsed"
                              Background="#2A2A2A"
                              Margin="0,0,0,10">
                    <ItemsControl x:Name="photoPreviewItems">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Width="100" 
                                        Height="75" 
                                        Margin="5" 
                                        CornerRadius="5"
                                        Background="#444444">
                                    <Grid>
                                        <Image Source="{Binding ThumbnailPath}" 
                                               Stretch="UniformToFill"/>
                                        
                                        <!-- Photo Type Badge -->
                                        <Border Background="{Binding TypeBadgeColor}" 
                                                CornerRadius="3"
                                                Padding="3,1"
                                                HorizontalAlignment="Right"
                                                VerticalAlignment="Top"
                                                Margin="3">
                                            <TextBlock Text="{Binding PhotoType}" 
                                                       Foreground="White" 
                                                       FontSize="9"
                                                       FontWeight="Bold"/>
                                        </Border>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
                
            </StackPanel>
        </ScrollViewer>

        <!-- Footer Buttons -->
        <Border Grid.Row="2" Background="#2A2A2A" Padding="15">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button x:Name="printButton" 
                        Content="Print" 
                        Click="PrintButton_Click" 
                        Style="{StaticResource ButtonStyle}"
                        IsEnabled="False"/>
                
                <Button Content="Cancel" 
                        Click="CancelButton_Click" 
                        Style="{StaticResource CancelButtonStyle}"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>