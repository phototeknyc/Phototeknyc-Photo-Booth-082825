<UserControl x:Class="Photobooth.Controls.TemplateBrowserOverlay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:Photobooth.Converters"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200">

    <UserControl.Resources>
        <!-- Converters -->
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter"/>
        <converters:BoolToOpacityConverter x:Key="BoolToOpacityConverter"/>

        <!-- Modern Dark Theme Styles -->
        <Style x:Key="ModernTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#404040"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#606060"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="CaretBrush" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <Grid>
                                <ScrollViewer x:Name="PART_ContentHost"
                                              Margin="{TemplateBinding Padding}"
                                              VerticalAlignment="Center"/>
                                <TextBlock x:Name="PlaceholderText"
                                           Text="Search templates..."
                                           Foreground="#808080"
                                           Margin="{TemplateBinding Padding}"
                                           VerticalAlignment="Center"
                                           IsHitTestVisible="False"
                                           Visibility="Collapsed"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="Text" Value=""/>
                                    <Condition Property="IsFocused" Value="False"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="PlaceholderText" Property="Visibility" Value="Visible"/>
                            </MultiTrigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#4CAF50"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CloseButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="Width" Value="40"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="20">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#60FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#4CAF50"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="30,12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#5CBF60"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#3A9F40"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <!-- Main Overlay -->
    <Grid x:Name="MainOverlay"
          Background="#CC000000"
          Visibility="Collapsed">

        <!-- Animation -->
        <Grid.Triggers>
            <EventTrigger RoutedEvent="Grid.Loaded">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                         From="0" To="1" Duration="0:0:0.3"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Grid.Triggers>

        <!-- Content Container -->
        <Border Background="#2D2D30"
                CornerRadius="20"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                Margin="40,20,40,20">
            <Border.Effect>
                <DropShadowEffect Color="Black" BlurRadius="25" ShadowDepth="10" Opacity="0.7"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Header with Management Buttons -->
                <Grid Grid.Row="0" Margin="30,30,30,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Template Management Buttons -->
                    <StackPanel Grid.Column="0"
                                Orientation="Horizontal">

                        <!-- New Template Button (always visible) -->
                        <Button x:Name="NewTemplateButton"
                                Content="➕ New Template"
                                Margin="0,0,15,0"
                                Padding="15,10"
                                Background="#4CAF50"
                                Foreground="White"
                                FontSize="16"
                                FontWeight="SemiBold"
                                MinWidth="140"
                                Height="44"
                                Click="NewTemplate_Click">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            CornerRadius="8">
                                        <ContentPresenter HorizontalAlignment="Center"
                                                          VerticalAlignment="Center"
                                                          Margin="{TemplateBinding Padding}"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#5CBF60"/>
                                        </Trigger>
                                        <Trigger Property="IsPressed" Value="True">
                                            <Setter Property="Background" Value="#3A9F40"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>

                        <!-- Import Template Button -->
                        <Button x:Name="ImportTemplateButton"
                                Content="📥 Import"
                                Margin="0,0,15,0"
                                Padding="15,10"
                                Background="#2196F3"
                                Foreground="White"
                                FontSize="16"
                                MinWidth="100"
                                Height="44"
                                Click="ImportTemplate_Click">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            CornerRadius="8">
                                        <ContentPresenter HorizontalAlignment="Center"
                                                          VerticalAlignment="Center"
                                                          Margin="{TemplateBinding Padding}"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#42A5F5"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>

                        <!-- Select Mode Button -->
                        <Button x:Name="SelectModeButton"
                                Content="☐ Select Mode"
                                Margin="0,0,15,0"
                                Padding="15,10"
                                Background="#9C27B0"
                                Foreground="White"
                                FontSize="16"
                                MinWidth="130"
                                Height="44"
                                Click="SelectModeButton_Click">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            CornerRadius="8">
                                        <ContentPresenter HorizontalAlignment="Center"
                                                          VerticalAlignment="Center"
                                                          Margin="{TemplateBinding Padding}"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#AB47BC"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>

                        <!-- Separator -->
                        <Border Width="1"
                                Height="30"
                                Background="#606060"
                                Margin="0,0,15,0"
                                VerticalAlignment="Center"/>

                        <!-- Select Mode Panel (visible when in select mode) -->
                        <StackPanel x:Name="SelectModePanel"
                                    Orientation="Horizontal"
                                    Visibility="Collapsed">

                            <Button x:Name="SelectAllButton"
                                    Content="☑ Select All"
                                    Margin="0,0,10,0"
                                    Padding="15,10"
                                    Background="#607D8B"
                                    Foreground="White"
                                    FontSize="16"
                                    MinWidth="100"
                                    Height="44"
                                    Click="SelectAllButton_Click">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                CornerRadius="8">
                                            <ContentPresenter HorizontalAlignment="Center"
                                                              VerticalAlignment="Center"
                                                              Margin="{TemplateBinding Padding}"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#78909C"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>

                            <Button x:Name="DeselectAllButton"
                                    Content="☐ Deselect All"
                                    Margin="0,0,10,0"
                                    Padding="15,10"
                                    Background="#607D8B"
                                    Foreground="White"
                                    FontSize="16"
                                    MinWidth="120"
                                    Height="44"
                                    Click="DeselectAllButton_Click">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                CornerRadius="8">
                                            <ContentPresenter HorizontalAlignment="Center"
                                                              VerticalAlignment="Center"
                                                              Margin="{TemplateBinding Padding}"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#78909C"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>

                            <TextBlock x:Name="SelectionStatusText"
                                       Text="0 selected"
                                       Foreground="#B0B0B0"
                                       FontSize="14"
                                       VerticalAlignment="Center"
                                       Margin="10,0,10,0"/>

                            <Button x:Name="DeleteSelectedButton"
                                    Content="🗑️ Delete Selected"
                                    Padding="15,10"
                                    Background="#F44336"
                                    Foreground="White"
                                    FontSize="16"
                                    MinWidth="140"
                                    Height="44"
                                    IsEnabled="False"
                                    Click="DeleteSelectedButton_Click">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                CornerRadius="8"
                                                Opacity="{Binding IsEnabled, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BoolToOpacityConverter}, FallbackValue=1}">
                                            <ContentPresenter HorizontalAlignment="Center"
                                                              VerticalAlignment="Center"
                                                              Margin="{TemplateBinding Padding}"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Trigger.EnterActions>
                                                    <BeginStoryboard>
                                                        <Storyboard>
                                                            <ColorAnimation Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                                            To="#EF5350" Duration="0:0:0.2"/>
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </Trigger.EnterActions>
                                            </Trigger>
                                            <Trigger Property="IsEnabled" Value="False">
                                                <Setter Property="Opacity" Value="0.5"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>

                            <!-- Separator -->
                            <Border Width="1"
                                    Height="30"
                                    Background="#606060"
                                    Margin="15,0,15,0"
                                    VerticalAlignment="Center"/>
                        </StackPanel>

                        <!-- Template-specific Management Buttons (visible when template selected) -->
                        <StackPanel x:Name="TemplateManagementButtons"
                                    Orientation="Horizontal"
                                    Visibility="Collapsed">

                            <Button x:Name="DuplicateTemplateButton"
                                    Content="📋 Duplicate"
                                    Margin="0,0,10,0"
                                    Padding="15,10"
                                    Background="#5CAF50"
                                    Foreground="White"
                                    FontSize="16"
                                    MinWidth="100"
                                    Height="44"
                                    Click="DuplicateTemplate_Click">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                CornerRadius="8">
                                            <ContentPresenter HorizontalAlignment="Center"
                                                              VerticalAlignment="Center"
                                                              Margin="{TemplateBinding Padding}"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#6CBF60"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>

                            <Button x:Name="RenameTemplateButton"
                                    Content="✏️ Rename"
                                    Margin="0,0,10,0"
                                    Padding="15,10"
                                    Background="#2196F3"
                                    Foreground="White"
                                    FontSize="16"
                                    MinWidth="100"
                                    Height="44"
                                    Click="RenameTemplate_Click">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                CornerRadius="8">
                                            <ContentPresenter HorizontalAlignment="Center"
                                                              VerticalAlignment="Center"
                                                              Margin="{TemplateBinding Padding}"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#42A5F5"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>

                            <Button x:Name="ExportTemplateButton"
                                    Content="📤 Export"
                                    Margin="0,0,10,0"
                                    Padding="15,10"
                                    Background="#FF9800"
                                    Foreground="White"
                                    FontSize="16"
                                    MinWidth="100"
                                    Height="44"
                                    Click="ExportTemplate_Click">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                CornerRadius="8">
                                            <ContentPresenter HorizontalAlignment="Center"
                                                              VerticalAlignment="Center"
                                                              Margin="{TemplateBinding Padding}"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#FFB74D"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>

                            <Button x:Name="DeleteTemplateButton"
                                    Content="🗑️ Delete"
                                    Padding="15,10"
                                    Background="#F44336"
                                    Foreground="White"
                                    FontSize="16"
                                    MinWidth="100"
                                    Height="44"
                                    Click="DeleteTemplate_Click">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                CornerRadius="8">
                                            <ContentPresenter HorizontalAlignment="Center"
                                                              VerticalAlignment="Center"
                                                              Margin="{TemplateBinding Padding}"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#EF5350"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </StackPanel>
                    </StackPanel>

                    <!-- Title -->
                    <TextBlock Text="Template Browser"
                               Grid.Column="1"
                               FontSize="32"
                               FontWeight="Bold"
                               Foreground="White"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"/>

                    <!-- Close Button -->
                    <Button x:Name="CloseButton"
                            Grid.Column="2"
                            Content="✕"
                            Style="{StaticResource CloseButtonStyle}"
                            HorizontalAlignment="Right"
                            Click="CloseButton_Click"/>
                </Grid>

                <!-- Search Bar -->
                <Grid Grid.Row="1" Margin="30,0,30,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBox x:Name="SearchTextBox"
                             Grid.Column="0"
                             Style="{StaticResource ModernTextBoxStyle}"
                             TextChanged="SearchTextBox_TextChanged"/>

                    <Button x:Name="ClearSearchButton"
                            Grid.Column="1"
                            Content="Clear"
                            Margin="10,0,0,0"
                            Padding="20,8"
                            Background="#606060"
                            Foreground="White"
                            FontSize="14"
                            Click="ClearSearchButton_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                        CornerRadius="8">
                                    <ContentPresenter HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"
                                                      Margin="{TemplateBinding Padding}"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#707070"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>

                <!-- Templates List -->
                <ScrollViewer Grid.Row="2"
                              VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled"
                              Margin="30,0,30,20">
                    <Grid>
                        <!-- Templates Grid -->
                        <ItemsControl x:Name="TemplatesList">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel Orientation="Horizontal"
                                               HorizontalAlignment="Center"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>

                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border x:Name="TemplateCard"
                                            Width="240"
                                            Height="280"
                                            Margin="10"
                                            Background="#3A3A3A"
                                            BorderBrush="#3A3A3A"
                                            BorderThickness="2"
                                            CornerRadius="10"
                                            MouseDown="TemplateCard_MouseDown"
                                            Tag="{Binding}"
                                            Cursor="Hand">
                                        <Border.Effect>
                                            <DropShadowEffect ShadowDepth="4" Opacity="0.3" BlurRadius="8"/>
                                        </Border.Effect>

                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="160"/>
                                                <RowDefinition Height="*"/>
                                            </Grid.RowDefinitions>

                                            <!-- Selection Checkbox (visible when in select mode) -->
                                            <Border Grid.Row="0"
                                                    HorizontalAlignment="Left"
                                                    VerticalAlignment="Top"
                                                    Margin="10"
                                                    Background="#CC000000"
                                                    CornerRadius="4"
                                                    Visibility="{Binding ShowCheckbox, Converter={StaticResource BoolToVisibilityConverter}}"
                                                    Panel.ZIndex="10">
                                                <CheckBox IsChecked="{Binding IsSelected}"
                                                          Margin="5"
                                                          Width="24"
                                                          Height="24">
                                                    <CheckBox.Template>
                                                        <ControlTemplate TargetType="CheckBox">
                                                            <Grid>
                                                                <Border x:Name="CheckBorder"
                                                                        Background="White"
                                                                        BorderBrush="#4CAF50"
                                                                        BorderThickness="2"
                                                                        CornerRadius="4"
                                                                        Width="24"
                                                                        Height="24">
                                                                    <Path x:Name="CheckMark"
                                                                          Data="M 2,12 L 8,18 L 20,4"
                                                                          Stroke="#4CAF50"
                                                                          StrokeThickness="3"
                                                                          Visibility="Collapsed"/>
                                                                </Border>
                                                            </Grid>
                                                            <ControlTemplate.Triggers>
                                                                <Trigger Property="IsChecked" Value="True">
                                                                    <Setter TargetName="CheckBorder" Property="Background" Value="#4CAF50"/>
                                                                    <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                                                                    <Setter TargetName="CheckMark" Property="Stroke" Value="White"/>
                                                                </Trigger>
                                                            </ControlTemplate.Triggers>
                                                        </ControlTemplate>
                                                    </CheckBox.Template>
                                                </CheckBox>
                                            </Border>

                                            <!-- Template Preview -->
                                            <Border Grid.Row="0"
                                                    Background="#2A2A2A"
                                                    Margin="10,10,10,5"
                                                    CornerRadius="8"
                                                    ClipToBounds="True">
                                                <Grid>
                                                    <!-- Thumbnail Image -->
                                                    <Image x:Name="TemplateThumbnail"
                                                           Source="{Binding ThumbnailImageSource}"
                                                           Stretch="Uniform"
                                                           Margin="5"/>

                                                    <!-- No Preview Fallback -->
                                                    <Grid x:Name="NoPreviewFallback"
                                                          Visibility="{Binding HasThumbnail, Converter={StaticResource InverseBoolToVisibilityConverter}, FallbackValue=Visible}">
                                                        <Rectangle Fill="#252525"/>
                                                        <StackPanel HorizontalAlignment="Center"
                                                                    VerticalAlignment="Center">
                                                            <TextBlock Text="📄"
                                                                       FontSize="48"
                                                                       HorizontalAlignment="Center"/>
                                                            <TextBlock Text="No Preview"
                                                                       FontSize="12"
                                                                       Foreground="#808080"
                                                                       Margin="0,10,0,0"/>
                                                        </StackPanel>
                                                    </Grid>

                                                    <!-- Template Type Badge -->
                                                    <Border Background="#000000AA"
                                                            HorizontalAlignment="Right"
                                                            VerticalAlignment="Top"
                                                            CornerRadius="0,0,0,8"
                                                            Padding="8,4">
                                                        <TextBlock Text="{Binding SizeText}"
                                                                   Foreground="White"
                                                                   FontSize="10"/>
                                                    </Border>
                                                </Grid>
                                            </Border>

                                            <!-- Template Info -->
                                            <Grid Grid.Row="1" Margin="12,5,12,12">
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="*"/>
                                                    <RowDefinition Height="Auto"/>
                                                </Grid.RowDefinitions>

                                                <!-- Name -->
                                                <TextBlock Grid.Row="0"
                                                           Text="{Binding Name}"
                                                           FontWeight="SemiBold"
                                                           FontSize="14"
                                                           Foreground="White"
                                                           TextTrimming="CharacterEllipsis"
                                                           ToolTip="{Binding Name}"/>

                                                <!-- Description -->
                                                <TextBlock Grid.Row="1"
                                                           Text="{Binding Description}"
                                                           FontSize="11"
                                                           Foreground="#B0B0B0"
                                                           TextTrimming="CharacterEllipsis"
                                                           Margin="0,2,0,0"/>

                                                <!-- Date -->
                                                <TextBlock Grid.Row="2"
                                                           Text="{Binding CreatedDate, StringFormat='Created: {0:MMM dd, yyyy}'}"
                                                           FontSize="10"
                                                           Foreground="#808080"
                                                           VerticalAlignment="Top"
                                                           Margin="0,4,0,0"/>

                                                <!-- Load Button -->
                                                <Button Grid.Row="3"
                                                        Content="Load Template"
                                                        HorizontalAlignment="Stretch"
                                                        Height="32"
                                                        Background="#4CAF50"
                                                        Foreground="White"
                                                        FontWeight="SemiBold"
                                                        Click="LoadTemplate_Click"
                                                        Tag="{Binding}">
                                                    <Button.Template>
                                                        <ControlTemplate TargetType="Button">
                                                            <Border Background="{TemplateBinding Background}"
                                                                    CornerRadius="6">
                                                                <ContentPresenter HorizontalAlignment="Center"
                                                                                  VerticalAlignment="Center"/>
                                                            </Border>
                                                            <ControlTemplate.Triggers>
                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                    <Setter Property="Background" Value="#5CBF60"/>
                                                                </Trigger>
                                                            </ControlTemplate.Triggers>
                                                        </ControlTemplate>
                                                    </Button.Template>
                                                </Button>
                                            </Grid>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <!-- No Templates Message -->
                        <TextBlock x:Name="NoTemplatesMessage"
                                   Text="No templates found. Create a new template or import existing ones."
                                   FontSize="18"
                                   Foreground="#808080"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   Visibility="Collapsed"/>
                    </Grid>
                </ScrollViewer>

                <!-- Action Buttons -->
                <Grid Grid.Row="3" Margin="30,0,30,30">
                    <StackPanel Orientation="Horizontal"
                                HorizontalAlignment="Right">
                        <Button x:Name="CancelButton"
                                Content="Cancel"
                                Width="120"
                                Margin="0,0,10,0"
                                Padding="12,10"
                                Background="#606060"
                                Foreground="White"
                                FontSize="16"
                                Click="CancelButton_Click">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            CornerRadius="8">
                                        <ContentPresenter HorizontalAlignment="Center"
                                                          VerticalAlignment="Center"
                                                          Margin="{TemplateBinding Padding}"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#707070"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>

                        <Button x:Name="LoadSelectedButton"
                                Content="Load Selected"
                                Width="150"
                                IsEnabled="False"
                                Style="{StaticResource ActionButtonStyle}"/>
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>