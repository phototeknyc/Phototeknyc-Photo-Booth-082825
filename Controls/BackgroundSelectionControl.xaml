<UserControl x:Class="Photobooth.Controls.BackgroundSelectionControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">

    <UserControl.Resources>
        <!-- Background Thumbnail Style -->
        <Style x:Key="BackgroundThumbnailStyle" TargetType="Border">
            <Setter Property="Width" Value="150"/>
            <Setter Property="Height" Value="100"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="BorderThickness" Value="3"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="#4CAF50"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Category Tab Style -->
        <Style x:Key="CategoryTabStyle" TargetType="RadioButton">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="border"
                                Background="#333"
                                BorderBrush="#555"
                                BorderThickness="1"
                                CornerRadius="5"
                                Padding="15,8"
                                Margin="3">
                            <TextBlock Text="{TemplateBinding Content}"
                                     Foreground="White"
                                     FontSize="14"
                                     HorizontalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#4CAF50"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#4CAF50"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#444"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="#2A2A2A">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#1E1E1E" Padding="15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Virtual Background Selection"
                             Foreground="White"
                             FontSize="18"
                             FontWeight="Bold"
                             VerticalAlignment="Center"/>

                    <CheckBox x:Name="EnableBackgroundRemovalCheckbox"
                            Content="Enable Background Removal"
                            Foreground="White"
                            FontSize="14"
                            Margin="30,0,0,0"
                            VerticalAlignment="Center"
                            IsChecked="True"
                            Checked="EnableBackgroundRemoval_Changed"
                            Unchecked="EnableBackgroundRemoval_Changed"/>
                </StackPanel>

                <Button Grid.Column="1"
                        x:Name="CloseButton"
                        Content="✕"
                        Width="30"
                        Height="30"
                        Background="Transparent"
                        Foreground="White"
                        BorderThickness="0"
                        FontSize="16"
                        Click="CloseButton_Click"/>
            </Grid>
        </Border>

        <!-- Category Tabs -->
        <ScrollViewer Grid.Row="1"
                     HorizontalScrollBarVisibility="Auto"
                     VerticalScrollBarVisibility="Disabled"
                     Margin="10,10,10,0">
            <StackPanel x:Name="CategoryTabsPanel"
                       Orientation="Horizontal">
                <!-- Categories will be added dynamically -->
            </StackPanel>
        </ScrollViewer>

        <!-- Background Grid -->
        <ScrollViewer Grid.Row="2"
                     VerticalScrollBarVisibility="Auto"
                     Margin="10">
            <WrapPanel x:Name="BackgroundsPanel"
                      Orientation="Horizontal"
                      HorizontalAlignment="Center">
                <!-- Background thumbnails will be added dynamically -->
            </WrapPanel>
        </ScrollViewer>

        <!-- Bottom Controls -->
        <Border Grid.Row="3" Background="#1E1E1E" Padding="15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Upload Custom Background -->
                <Button Grid.Column="0"
                        x:Name="UploadButton"
                        Content="Upload Custom"
                        Width="120"
                        Height="35"
                        Background="#4CAF50"
                        Foreground="White"
                        BorderThickness="0"
                        FontSize="14"
                        Click="UploadButton_Click"/>

                <!-- Preview Toggle -->
                <CheckBox Grid.Column="1"
                         x:Name="LivePreviewCheckbox"
                         Content="Live Preview"
                         Foreground="White"
                         FontSize="14"
                         HorizontalAlignment="Center"
                         VerticalAlignment="Center"
                         IsChecked="True"
                         Checked="LivePreview_Changed"
                         Unchecked="LivePreview_Changed"/>

                <!-- Apply Button -->
                <Button Grid.Column="2"
                        x:Name="ApplyButton"
                        Content="Apply Background"
                        Width="120"
                        Height="35"
                        Background="#2196F3"
                        Foreground="White"
                        BorderThickness="0"
                        FontSize="14"
                        Click="ApplyButton_Click"/>
            </Grid>
        </Border>

        <!-- Loading Overlay -->
        <Border x:Name="LoadingOverlay"
               Grid.RowSpan="4"
               Background="#80000000"
               Visibility="Collapsed">
            <StackPanel HorizontalAlignment="Center"
                       VerticalAlignment="Center">
                <ProgressBar Width="200" Height="20" IsIndeterminate="True"/>
                <TextBlock Text="Processing..."
                         Foreground="White"
                         FontSize="16"
                         Margin="0,10,0,0"
                         HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>