<UserControl x:Class="Photobooth.Controls.PinEntryOverlay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200">
    
    <UserControl.Resources>
        <!-- Modern Dark Theme Styles matching Settings Overlay -->
        <Style x:Key="ModernPinButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#2A2A2A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Width" Value="85"/>
            <Setter Property="Height" Value="85"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                CornerRadius="12"
                                RenderTransformOrigin="0.5,0.5">
                            <Border.RenderTransform>
                                <ScaleTransform x:Name="borderScale" ScaleX="1" ScaleY="1"/>
                            </Border.RenderTransform>
                            <Border.Effect>
                                <DropShadowEffect Color="Black" BlurRadius="5" ShadowDepth="2" Opacity="0.2"/>
                            </Border.Effect>
                            <Grid>
                                <ContentPresenter HorizontalAlignment="Center" 
                                                VerticalAlignment="Center"/>
                                <!-- Hover Overlay -->
                                <Border x:Name="hoverOverlay" 
                                        Background="#4CAF50" 
                                        Opacity="0" 
                                        CornerRadius="12"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="hoverOverlay"
                                                           Storyboard.TargetProperty="Opacity"
                                                           To="0.2" Duration="0:0:0.2"/>
                                            <DoubleAnimation Storyboard.TargetName="borderScale"
                                                           Storyboard.TargetProperty="ScaleX"
                                                           To="1.05" Duration="0:0:0.1"/>
                                            <DoubleAnimation Storyboard.TargetName="borderScale"
                                                           Storyboard.TargetProperty="ScaleY"
                                                           To="1.05" Duration="0:0:0.1"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="hoverOverlay"
                                                           Storyboard.TargetProperty="Opacity"
                                                           To="0" Duration="0:0:0.2"/>
                                            <DoubleAnimation Storyboard.TargetName="borderScale"
                                                           Storyboard.TargetProperty="ScaleX"
                                                           To="1" Duration="0:0:0.1"/>
                                            <DoubleAnimation Storyboard.TargetName="borderScale"
                                                           Storyboard.TargetProperty="ScaleY"
                                                           To="1" Duration="0:0:0.1"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="hoverOverlay" Property="Opacity" Value="0.4"/>
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="borderScale"
                                                           Storyboard.TargetProperty="ScaleX"
                                                           To="0.95" Duration="0:0:0.05"/>
                                            <DoubleAnimation Storyboard.TargetName="borderScale"
                                                           Storyboard.TargetProperty="ScaleY"
                                                           To="0.95" Duration="0:0:0.05"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="borderScale"
                                                           Storyboard.TargetProperty="ScaleX"
                                                           To="1" Duration="0:0:0.05"/>
                                            <DoubleAnimation Storyboard.TargetName="borderScale"
                                                           Storyboard.TargetProperty="ScaleY"
                                                           To="1" Duration="0:0:0.05"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Special Button Styles -->
        <Style x:Key="SpecialPinButtonStyle" TargetType="Button" BasedOn="{StaticResource ModernPinButtonStyle}">
            <Setter Property="Background" Value="#353535"/>
            <Setter Property="FontSize" Value="20"/>
        </Style>

        <!-- Action Button Style -->
        <Style x:Key="ModernActionButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="50"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                CornerRadius="25">
                            <Border.Effect>
                                <DropShadowEffect Color="Black" BlurRadius="8" ShadowDepth="3" Opacity="0.3"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                           To="0.9" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                           To="1" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.98" ScaleY="0.98"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
        </Style>
    </UserControl.Resources>

    <!-- Main Overlay Grid -->
    <Grid x:Name="MainOverlay" 
          Background="#CC000000" 
          Visibility="Collapsed">
        
        <!-- Animation -->
        <Grid.Triggers>
            <EventTrigger RoutedEvent="Grid.Loaded">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                         From="0" To="1" Duration="0:0:0.3"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Grid.Triggers>
        
        <!-- PIN Entry Panel -->
        <Border x:Name="PinPanel"
                Background="#2D2D30" 
                CornerRadius="20"
                Width="450"
                MaxHeight="700"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
            
            <Border.Effect>
                <DropShadowEffect Color="Black" BlurRadius="25" ShadowDepth="10" Opacity="0.7"/>
            </Border.Effect>
            
            <!-- Scale Animation -->
            <Border.RenderTransform>
                <ScaleTransform x:Name="PanelScale" ScaleX="1" ScaleY="1" CenterX="225" CenterY="350"/>
            </Border.RenderTransform>
            
            <Border.Triggers>
                <EventTrigger RoutedEvent="Border.Loaded">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetName="PanelScale"
                                           Storyboard.TargetProperty="ScaleX"
                                           From="0.8" To="1" Duration="0:0:0.3">
                                <DoubleAnimation.EasingFunction>
                                    <BackEase EasingMode="EaseOut" Amplitude="0.3"/>
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                            <DoubleAnimation Storyboard.TargetName="PanelScale"
                                           Storyboard.TargetProperty="ScaleY"
                                           From="0.8" To="1" Duration="0:0:0.3">
                                <DoubleAnimation.EasingFunction>
                                    <BackEase EasingMode="EaseOut" Amplitude="0.3"/>
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Border.Triggers>
            
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <!-- Header with Close Button -->
                <Grid Grid.Row="0" Margin="30,25,30,0">
                    <StackPanel>
                        <!-- Title -->
                        <TextBlock x:Name="TitleText"
                                  Text="Unlock Interface"
                                  FontSize="28"
                                  FontWeight="Light"
                                  Foreground="White"
                                  HorizontalAlignment="Center"/>
                        
                        <!-- Message -->
                        <TextBlock x:Name="MessageText"
                                  Text="Enter PIN to unlock settings"
                                  FontSize="14"
                                  Foreground="#A0A0A0"
                                  HorizontalAlignment="Center"
                                  Margin="0,8,0,0"/>
                    </StackPanel>
                    
                    <!-- Close Button -->
                    <Button x:Name="CloseButton"
                            Content="✕"
                            FontSize="20"
                            Width="35"
                            Height="35"
                            Background="Transparent"
                            Foreground="#808080"
                            BorderThickness="0"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Top"
                            Margin="0,-5,-5,0"
                            Cursor="Hand"
                            Click="CancelButton_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                        CornerRadius="17.5">
                                    <ContentPresenter HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#40FFFFFF"/>
                                        <Setter Property="Foreground" Value="White"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>
                
                <!-- PIN Display -->
                <Border Grid.Row="2"
                        Background="#1A1A1A"
                        CornerRadius="12"
                        Width="340"
                        Height="65"
                        Margin="0,25,0,30">
                    <Grid>
                        <!-- PIN Dots/Numbers -->
                        <TextBlock x:Name="PinDisplay"
                                  Text=""
                                  FontSize="36"
                                  FontFamily="Segoe UI"
                                  FontWeight="Light"
                                  Foreground="#4CAF50"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center"/>
                        
                        <!-- Placeholder when empty -->
                        <TextBlock x:Name="PinPlaceholder"
                                  Text="• • • •"
                                  FontSize="24"
                                  Foreground="#505050"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center"
                                  Visibility="Visible"/>
                    </Grid>
                </Border>
                
                <!-- Number Pad -->
                <Grid Grid.Row="3" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <!-- Row 1: 1,2,3 -->
                    <Button Grid.Row="0" Grid.Column="0" Content="1" Style="{StaticResource ModernPinButtonStyle}" Click="PinButton_Click"/>
                    <Button Grid.Row="0" Grid.Column="1" Content="2" Style="{StaticResource ModernPinButtonStyle}" Click="PinButton_Click"/>
                    <Button Grid.Row="0" Grid.Column="2" Content="3" Style="{StaticResource ModernPinButtonStyle}" Click="PinButton_Click"/>
                    
                    <!-- Row 2: 4,5,6 -->
                    <Button Grid.Row="1" Grid.Column="0" Content="4" Style="{StaticResource ModernPinButtonStyle}" Click="PinButton_Click"/>
                    <Button Grid.Row="1" Grid.Column="1" Content="5" Style="{StaticResource ModernPinButtonStyle}" Click="PinButton_Click"/>
                    <Button Grid.Row="1" Grid.Column="2" Content="6" Style="{StaticResource ModernPinButtonStyle}" Click="PinButton_Click"/>
                    
                    <!-- Row 3: 7,8,9 -->
                    <Button Grid.Row="2" Grid.Column="0" Content="7" Style="{StaticResource ModernPinButtonStyle}" Click="PinButton_Click"/>
                    <Button Grid.Row="2" Grid.Column="1" Content="8" Style="{StaticResource ModernPinButtonStyle}" Click="PinButton_Click"/>
                    <Button Grid.Row="2" Grid.Column="2" Content="9" Style="{StaticResource ModernPinButtonStyle}" Click="PinButton_Click"/>
                    
                    <!-- Row 4: Clear,0,Delete -->
                    <Button Grid.Row="3" Grid.Column="0" Style="{StaticResource SpecialPinButtonStyle}" Click="ClearButton_Click">
                        <TextBlock Text="CLEAR" FontSize="14" FontWeight="Medium"/>
                    </Button>
                    <Button Grid.Row="3" Grid.Column="1" Content="0" Style="{StaticResource ModernPinButtonStyle}" Click="PinButton_Click"/>
                    <Button Grid.Row="3" Grid.Column="2" Style="{StaticResource SpecialPinButtonStyle}" Click="BackspaceButton_Click">
                        <TextBlock Text="⌫" FontSize="24"/>
                    </Button>
                </Grid>
                
                <!-- Action Buttons -->
                <Grid Grid.Row="4" Margin="40,10,40,35">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <Button Grid.Column="0" 
                            x:Name="CancelButton"
                            Content="CANCEL" 
                            Style="{StaticResource ModernActionButtonStyle}"
                            Background="#607D8B"
                            Click="CancelButton_Click"/>
                    
                    <Button Grid.Column="2" 
                            x:Name="SubmitButton"
                            Content="UNLOCK" 
                            Style="{StaticResource ModernActionButtonStyle}"
                            Background="#4CAF50"
                            Click="SubmitButton_Click"/>
                </Grid>
            </Grid>
        </Border>
        
        <!-- Error Message Toast -->
        <Border x:Name="ErrorMessageBorder"
                Background="#F44336"
                CornerRadius="25"
                Padding="25,12"
                HorizontalAlignment="Center"
                VerticalAlignment="Bottom"
                Margin="0,0,0,50"
                Visibility="Collapsed"
                Opacity="0">
            <Border.Effect>
                <DropShadowEffect Color="Black" BlurRadius="15" ShadowDepth="5" Opacity="0.5"/>
            </Border.Effect>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="⚠" FontSize="18" Foreground="White" Margin="0,0,10,0" VerticalAlignment="Center"/>
                <TextBlock x:Name="ErrorMessageText"
                          Text="Incorrect PIN"
                          FontSize="14"
                          FontWeight="Medium"
                          Foreground="White"
                          VerticalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>