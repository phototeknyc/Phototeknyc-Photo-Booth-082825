<Page x:Class="Photobooth.Pages.Setting"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Photobooth.Pages"
      xmlns:nav="clr-namespace:Photobooth.Resources.Controls"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
Background="#333333">
    <Grid>
        <!--create a menu bar at side which shows background to change-->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
           
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border BorderBrush="#ccc" Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="2" BorderThickness="0,0,0,0.5">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="Setting" FontSize="30" Foreground="#FFFFFF" FontWeight="Bold"/>
            </StackPanel>
        </Border>
        <Border BorderBrush="#ccc" Grid.Column="0" Grid.Row="1" BorderThickness="0,0,0.5,0">
            <ScrollViewer  VerticalScrollBarVisibility="Auto">
                <StackPanel  VerticalAlignment="Top" HorizontalAlignment="Left"  Orientation="Vertical"  >
                    <Expander Header="Style" IsExpanded="True" Foreground="#FFFFFF" FontSize="16">
                        <ListBox x:Name="styleMenu" SelectionMode="Single" Background="{x:Null}" BorderThickness="0" Width="155" Foreground="#FFFFFF" Margin="30,0,0,0" SelectionChanged="ListBox_SelectionChanged">
                            <nav:NavButton Label="Background" NavLink="Pages/BackgroundSettingControl.xaml"/>
                        </ListBox>

                    </Expander>
                </StackPanel>
            </ScrollViewer>
        </Border>
        <Frame Grid.Row="1" Grid.Column="1" x:Name="frame" NavigationUIVisibility="Hidden"/>


    </Grid>
</Page>
