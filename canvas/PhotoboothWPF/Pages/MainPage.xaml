<Page x:Class="PhotoboothWPF.Pages.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PhotoboothWPF.Pages" xmlns:controls="clr-namespace:DesignerCanvas.Controls;assembly=DesignerCanvas"
      mc:Ignorable="d" 
      MinWidth="1200" MinHeight="700" Background="#333333"
      Title="MainPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*" MinWidth="500"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Border Grid.Column="0"  Grid.Row="0" BorderBrush="#CCCCCC" BorderThickness="0,0,0.5,0" >
            <Frame Source="Sidebar.xaml"/>
        </Border>
        <StackPanel  Grid.Column="1" Grid.Row="0" >
           
               
                <StackPanel Grid.Column="0" Grid.Row="0" Background="WhiteSmoke" Margin="50,50,50,50"  VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
                    <controls:DesignerCanvas x:Name="dcvs"  x:FieldModifier="public" Height="500" Width="500" ShowBoundaries="false" VerticalAlignment="Center" Background="WhiteSmoke"/>
                    <!-- width will be recalculated when program will run-->
                </StackPanel>
            
        </StackPanel>
        <!--<Frame  Grid.Column="1" Grid.Row="0" Background="WhiteSmoke" Margin="0,10" x:Name="frame"  MinWidth="500" MinHeight="300" NavigationUIVisibility="Hidden" />-->
        <Border Grid.Column="2" Grid.Row="0"  Grid.ColumnSpan="3"  BorderBrush="#CCCCCC" Width="200" BorderThickness="0.5,0,0,0">
            <Frame x:Name="PropertiesPanel" Source="Properties.xaml"/>
        </Border>
    </Grid>
</Page>
