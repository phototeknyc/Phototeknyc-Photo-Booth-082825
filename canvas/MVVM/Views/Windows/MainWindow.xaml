<Window x:Class="Photobooth.MVVM.Views.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Photobooth.MVVM.Views.Windows" 
        xmlns:userControls="clr-namespace:Photobooth.MVVM.Views.UserControls"
        mc:Ignorable="d"
        Title="PhotoBooth" WindowState="Maximized" ScrollViewer.HorizontalScrollBarVisibility="Hidden" ScrollViewer.CanContentScroll="True" MinWidth="1200" MinHeight="720" Icon="../../../images/CameraIcon.png" WindowStartupLocation="CenterScreen">
    <Grid Background="{DynamicResource BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*" MinWidth="500"/>
        </Grid.ColumnDefinitions>

        <userControls:TopBar Grid.Row="0" Grid.ColumnSpan="2" x:Name="TopBar" />
        <userControls:MainLeftSideBar Grid.Column="0" Grid.Row="1" x:Name="SidebarControl"/>
        <ContentControl Grid.Column="1" Grid.Row="1" Name="MainContentControl"/>
    </Grid>
</Window>
