<UserControl x:Class="Photobooth.MVVM.Views.UserControls.Home.HomeMain"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Photobooth.MVVM.Views.UserControls.Home"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/MVVM/Resources/Styles/Button.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="20">
            <Image x:Name="logoImage" Source="/images/CameraIcon.png" Width="50" Height="50"/>
            <TextBlock x:Name="headerText" Text="Photobooth" FontSize="30" VerticalAlignment="Center" Foreground="{DynamicResource ForegroundBrush}" Margin="10,0,0,0"/>
        </StackPanel>
        <Grid x:Name="buttonsGrid" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Button Content="Take a Photo" Width="150" Height="30" Command="{Binding NavigateToCameraCommand}" Cursor="Hand" Style="{StaticResource {x:Type Button}}" Grid.Row="0" Margin="5"/>
            <Button Content="View Gallery" Width="150" Height="30" Command="{Binding NavigateToGalleryCommand}" Cursor="Hand" Style="{StaticResource {x:Type Button}}" Grid.Row="1" Margin="5"/>
            <Button Content="Settings" Width="150" Height="30" Command="{Binding NavigateToSettingsCommand}" Cursor="Hand" Style="{StaticResource {x:Type Button}}" Grid.Row="2" Margin="5"/>
        </Grid>
    </Grid>
</UserControl>
