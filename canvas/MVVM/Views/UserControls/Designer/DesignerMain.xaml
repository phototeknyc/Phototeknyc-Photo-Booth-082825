<UserControl x:Class="Photobooth.MVVM.Views.UserControls.Designer.DesignerMain"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Photobooth.MVVM.Views.UserControls.Designer"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*" MinWidth="500"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        
        <Border Grid.Column="0" BorderBrush="#CCCCCC" BorderThickness="0,0,0.5,0" >
            <ContentControl x:Name="LeftSideBarControl" />
        </Border>
        
        <Grid Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                <ContentControl Content="{Binding CustomDesignerCanvas}" VerticalAlignment="Top" HorizontalAlignment="Left" Background="{DynamicResource ForegroundBrush}" Margin="10"/>
            </ScrollViewer>
        </Grid>

        <Border Grid.Column="2"  BorderBrush="#CCCCCC" Width="200" BorderThickness="0.5,0,0,0" Visibility="{Binding IsRightSidebarVisible}">
            <ContentControl x:Name="RightSideBarControl" />
        </Border>
    </Grid>
</UserControl>
